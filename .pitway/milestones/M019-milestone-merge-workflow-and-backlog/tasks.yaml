schema_version: 1
tasks:
  - id: T001
    name: milestone-merge core (git safety, idempotency, journal)
    objective: "Implement the core of pitway milestone-merge in
      src/core/milestones/merge.ts: mergeMilestone(root, milestoneId, { target?
      }). Read and resolve every needed piece of state (contract frontmatter,
      base_branch as the target default, the completion commit SHA via
      findCompletionCommit) from the CURRENT branch before any checkout runs;
      refuse by name when the completion commit cannot be found in current
      HEAD's history (wrong branch / detached HEAD) -- never a blanket
      assertOnMilestoneBranch check, which would incorrectly reject the
      legitimate idempotent re-run from the target branch post-merge. Refuse
      (before mutation) unless status is completed, unless base_branch is
      non-null, unless the working tree is clean, unless the resolved target
      branch exists, unless the milestone's own source branch exists, and unless
      MERGE_HEAD is absent (a distinct, actionable message for an interrupted
      prior merge). Add checkoutBranch to src/git/branch.ts and a new
      src/git/merge.ts (mergeBranches, abortMerge, isAncestor via git merge-base
      --is-ancestor). Idempotency: short-circuit to 'already merged' when the
      completion commit SHA is already an ancestor of the target branch. Wrap
      the merge attempt in a try/finally that restores the original branch on
      ANY thrown error, not only a conflict. On conflict: git merge --abort,
      restore, refuse. On success: merge commit message 'merge: <id> <title>',
      no PitWay trailer. Add a new sibling journal kind milestone_merge to
      src/state/journal.ts's discriminated union (never a
      journalOperationTypeSchema entry, never checkpoint-eligible) plus
      appendMilestoneMergeRecord, and call it on both the success and
      already-merged paths (AC001, AC002, AC003, AC004, AC005, AC006, AC007)."
    status: completed
    depends_on: []
    acceptance_criteria:
      - mergeMilestone refuses with a named error when the milestone's status is
        not completed, before any git command runs
      - A test proves every state read (contract frontmatter, base_branch,
        completion commit SHA) completes before the first git checkout call --
        e.g. by asserting the resolved values are correct even when the target
        branch's own .pitway/ directory is absent or stale
      - "Wrong-branch / detached-HEAD: a test proves the command refuses by name
        when the completion commit is unreachable from current HEAD (run from an
        unrelated branch, or detached HEAD without the commit), and separately
        proves the legitimate case succeeds -- a re-run from the target branch
        after a successful merge, where the commit IS reachable via the target's
        own ancestry"
      - Defaults --target to contract.frontmatter.base_branch; refuses with a
        distinct, named error when base_branch is null
      - Refuses on a dirty working tree, when the resolved target branch does
        not exist, and when the milestone's own source branch does not exist,
        all before any checkout
      - Refuses with a distinct, actionable message (not a generic dirty-tree
        message) when MERGE_HEAD is already present at command start
      - "On a merge conflict: git merge --abort is invoked, the original branch
        is re-checked-out, and the command refuses -- a test asserts the
        repository's branch and tree are unchanged after the refusal"
      - A test proves the original branch is restored via try/finally after an
        injected NON-conflict failure during the merge attempt, not only after a
        conflict
      - "Idempotency: a test proves that once the completion commit SHA is an
        ancestor of the target branch, a second invocation reports 'already
        merged' and creates no new commit"
      - "A successful merge produces a commit on the target branch with message
        'merge: <id> <title>' and no PitWay-Milestone/PitWay-Task trailer"
      - journalRecordSchema gains a milestone_merge sibling kind
        (id/milestone/targetBranch/mergeCommitSha/alreadyMerged/at);
        appendMilestoneMergeRecord is called on every completed merge (success
        or already-merged); a test proves it is never checkpoint-eligible
        (excluded by derivePending's kind === 'entry' filter) and needs no
        resolveTargetPath case
    write_scope:
      - src/git/branch.ts
      - src/git/merge.ts
      - src/core/milestones/merge.ts
      - src/state/journal.ts
      - tests/unit/journal.test.ts
      - tests/integration/milestone-merge.test.ts
    verification:
      strategy: tdd
      detail: npm test -- tests/unit/journal.test.ts
        tests/integration/milestone-merge.test.ts
    result:
      summary: "[truncated] ) with | checkoutBranch (src/git/branch.ts) and new
        src/git/merge.ts |
        (mergeBranches/abortMerge/isAncestor/isMergeInProgress); added the |
        milestone_merge sibling journal kind + appendMilestoneMergeRecord. |
        Covers AC001-AC007 including every architect/devops review-driven |
        refinement."
      evidence: Tests  66 passed (66) | Start at  23:19:45 | Duration  25.87s
        (transform 364ms, setup 0ms, import 524ms, tests 34.35s, environment
        0ms)
    attempts: 1
    usage:
      total_tokens: 197195
  - id: T002
    name: milestone-merge CLI command and shipped docs
    objective: Wire pitway milestone-merge <id> [--target <branch>] in
      src/cli/commands/milestone-merge.ts, registered in src/cli/index.ts,
      following the existing milestone-* command file conventions (pitway_result
      JSON envelope under --json, human-readable text otherwise, on every
      success/already-merged/refusal path). Ship
      src/integrations/claude/commands/milestone-merge.md (PitWay:-prefixed
      description, mirrored into .claude/ by pitway init's existing
      recursive-discovery copy -- no new copy-script logic); add a mention to
      src/integrations/claude/protocol-driver.md naming milestone-merge as the
      developer-invoked step after milestone-complete; document the command in
      USAGE.md. Update tests/integration/cli.test.ts's ALL_COMMAND_NAMES and
      'registers all N commands' count (24 -> 25) and
      tests/integration/build-bin.test.ts's real-binary command list (AC001,
      AC008, AC009).
    status: completed
    depends_on:
      - T001
    acceptance_criteria:
      - pitway milestone-merge <id> [--target <branch>] is registered and
        reachable; every success/already-merged/refusal path emits a
        pitway_result-shaped envelope under --json and readable text otherwise
      - tests/integration/cli.test.ts's ALL_COMMAND_NAMES includes
        milestone-merge and the registered-command count reads 25
      - tests/integration/build-bin.test.ts's real-compiled-binary command list
        is updated identically
      - src/integrations/claude/commands/milestone-merge.md exists with a
        PitWay:-prefixed description and is picked up by pitway init's existing
        asset discovery (tests/unit/claude-assets.test.ts)
      - protocol-driver.md names milestone-merge as the step after
        milestone-complete; USAGE.md documents the command and its --target
        default
    write_scope:
      - src/cli/commands/milestone-merge.ts
      - src/cli/index.ts
      - src/integrations/claude/commands/milestone-merge.md
      - src/integrations/claude/protocol-driver.md
      - USAGE.md
      - tests/integration/cli.test.ts
      - tests/integration/build-bin.test.ts
      - tests/integration/milestone-merge.test.ts
      - tests/unit/claude-assets.test.ts
    verification:
      strategy: tdd
      detail: npm test -- tests/integration/cli.test.ts
        tests/integration/build-bin.test.ts
        tests/integration/milestone-merge.test.ts
        tests/unit/claude-assets.test.ts
    result:
      summary: "[truncated]  <id> [--target <branch>] into the CLI |
        (src/cli/commands/milestone-merge.ts), mirroring existing milestone-* |
        conventions (generic error boundary, pitway_result envelope). Shipped |
        its command doc, updated USAGE.md/protocol-driver.md, and updated |
        registered-command counts (24->25)."
      evidence: Tests  116 passed (116) | Start at  23:57:13 | Duration  32.42s
        (transform 775ms, setup 0ms, import 1.08s, tests 36.89s, environment
        0ms)
    attempts: 1
    usage:
      total_tokens: 113117
  - id: T003
    name: "B001 -- PitWay: description prefix and ms-* milestone aliases"
    objective: "Prefix every PitWay-owned command doc's description frontmatter (all
      files under src/integrations/claude/commands/, including
      milestone-merge.md from T002) with 'PitWay: ', verbatim before the
      existing text -- excluding the 6 vendored skills under
      src/integrations/claude/skills/ (disclosed deviation, AC009). Add a native
      Commander .alias('ms-<verb>') to the 7 commands in
      src/cli/commands/milestone-add.ts, milestone-cancel.ts,
      milestone-complete.ts, milestone-confirm.ts, milestone-list.ts,
      milestone-review.ts, milestone-status.ts. Ship 7 new alias command docs
      (ms-add.md, ms-cancel.md, ms-complete.md, ms-confirm.md, ms-list.md,
      ms-review.md, ms-status.md) under src/integrations/claude/commands/, each
      byte-identical in body to its canonical counterpart. Update
      tests/integration/cli.test.ts and tests/integration/build-bin.test.ts for
      the new aliases and tests/unit/claude-assets.test.ts for the 7 new shipped
      files (AC009, AC010, AC011, AC012, AC013)."
    status: ready
    depends_on:
      - T002
    acceptance_criteria:
      - "Every command doc under src/integrations/claude/commands/ (24 files
        including T002's milestone-merge.md before this task's own aliases are
        added; 31 once this task's 7 ms-*.md files exist -- the same test sweeps
        in files this task itself creates) has a description frontmatter field
        starting with 'PitWay: '; the 6 files under
        src/integrations/claude/skills/ are unchanged -- a test asserts both
        halves of this directly"
      - pitway
        ms-add|ms-cancel|ms-complete|ms-confirm|ms-list|ms-review|ms-status each
        invoke identically to their milestone-* counterpart (including
        milestone-review's start/brief/record/report/decide subcommands under
        the ms-review alias); a test asserts each alias via Command.aliases()
      - program.commands count / ALL_COMMAND_NAMES is unchanged by this task
        (aliases add no new top-level entries) -- still 25 from T002
      - Each of the 7 ms-*.md files is byte-identical in body to its canonical
        milestone-*.md counterpart, asserted by a direct file-comparison test
      - milestone-merge has no ms-merge alias; no task-* command gains an alias;
        auto-run is unchanged -- a test proves the alias set is exactly the 7
        named commands, no more
    write_scope:
      - src/integrations/claude/commands
      - src/cli/commands/milestone-add.ts
      - src/cli/commands/milestone-cancel.ts
      - src/cli/commands/milestone-complete.ts
      - src/cli/commands/milestone-confirm.ts
      - src/cli/commands/milestone-list.ts
      - src/cli/commands/milestone-review.ts
      - src/cli/commands/milestone-status.ts
      - tests/integration/cli.test.ts
      - tests/integration/build-bin.test.ts
      - tests/unit/claude-assets.test.ts
    verification:
      strategy: tdd
      detail: npm test -- tests/integration/cli.test.ts
        tests/integration/build-bin.test.ts tests/unit/claude-assets.test.ts
    result: null
    usage: null
  - id: T004
    name: B002 -- reviewer role selection display labels
    objective: "Add a short (3-8 word) label field to each of the 9 REVIEW_ROLES
      entries in src/core/reviews/roles.ts (additive, alongside the existing
      id/focus -- focus itself is never edited). Update formatRoleList() in
      src/cli/review-prompt.ts to render '<Capitalized Role Name>: <label>' in
      place of the current raw 'id -- focus' line;
      parseSelection/promptForRoles's selection logic is unchanged. No checkbox
      or HTML markup; no ANSI bold by default (AC014, AC015, AC016)."
    status: completed
    depends_on: []
    acceptance_criteria:
      - Every REVIEW_ROLES entry has a new label field, 3-8 words, distinct from
        and additional to its existing focus string; focus's own value is
        byte-for-byte unchanged (a test diffs it against the current value)
      - The 9 role ids are unchanged -- no role added, removed, or renamed
      - "formatRoleList() renders each role as '<Role Name>: <label>' (role name
        capitalized, colon separator), with no '[ ]' or '<b>' markup anywhere in
        its output"
      - promptForRoles/parseSelection's numeric-selection behavior is unchanged
        by this task (same inputs still select the same roles)
    write_scope:
      - src/core/reviews/roles.ts
      - src/cli/review-prompt.ts
      - tests/unit/review-roles.test.ts
      - tests/integration/milestone-review-interactive.test.ts
    verification:
      strategy: tdd
      detail: npm test -- tests/unit/review-roles.test.ts
        tests/integration/milestone-review-interactive.test.ts
    result:
      summary: |
        Added a label field (3-8 word display description) to all 9 REVIEW_ROLES
        entries, focus/ids untouched, and updated formatRoleList() to render
        "RoleName: label" instead of the raw "id -- focus" line, no
        checkbox/HTML markup, selection logic unchanged.
      evidence: Tests  10 passed (10) | Start at  23:08:34 | Duration  2.10s
        (transform 280ms, setup 0ms, import 386ms, tests 1.65s, environment 0ms)
    attempts: 1
    usage:
      total_tokens: 78205
  - id: T005
    name: B003 -- backlog storage scalability investigation
    objective: "Write docs/evidence/M019/backlog-scalability.md: evaluate
      .pitway/backlog.yaml's current single-file, whole-load/whole-save design
      (loadBacklog/saveBacklog, src/state/store.ts) against realistic PitWay
      usage -- a single-developer-in-the-loop, human-paced workflow tool -- and
      reach an explicit, justified conclusion. No production code change is made
      in this task regardless of the conclusion reached (AC017)."
    status: completed
    depends_on: []
    acceptance_criteria:
      - docs/evidence/M019/backlog-scalability.md exists and reasons from the
        current implementation (confirmed by inspection, cited directly) rather
        than assumption
      - The document reaches and justifies an explicit conclusion, honestly
        stated even if it is 'no redesign is warranted at current/foreseeable
        scale'
      - No file outside docs/evidence/M019/ is modified by this task
    write_scope:
      - docs/evidence/M019/backlog-scalability.md
    verification:
      strategy: manual
      detail: developer/driver review per CT011's instruction
    result:
      summary: |
        Investigated .pitway/backlog.yaml's single-file whole-load/whole-save
        design against PitWay's real single-developer, sequential-write usage;
        concluded no redesign is warranted at current or foreseeable scale.
      evidence: |
        docs/evidence/M019/backlog-scalability.md, driver-reviewed: cites
        loadBacklog/saveBacklog (store.ts:240-250), confirms every .pitway/
        state file shares the same whole-file convention, cites real data (4
        items/66 lines after 18 milestones), and reasons from PitWay's
        sequential/no-multi-writer execution model. Explicit conclusion reached;
        no production code change made.
    attempts: 1
    usage:
      total_tokens: 65820
  - id: T006
    name: Backlog promotion dogfooding and roadmap reconciliation
    objective: "After this milestone is confirmed: run pitway backlog promote B001
      --task T003, backlog promote B002 --task T004, and backlog promote B003
      --task T005 for real against the live .pitway/backlog.yaml -- the first
      real exercise of backlog promote since M018 shipped it. These calls need
      no .pitway/backlog.yaml entry in this task's write_scope: a pending
      backlog_recording journal entry is already classified as an expected dirty
      path by the existing, unmodified classifyDirtyPaths({ journalMilestone })
      allowlist, mirroring M018's own precedent exactly -- no task in M018 ever
      declared backlog.yaml in write_scope either. All three promote calls ride
      along in this task's own single completion commit alongside its
      IMPLEMENTATION_PLAN.md change; no dedicated commit is created for them.
      Then reconcile IMPLEMENTATION_PLAN.md: the command-surface count (24 ->
      25), the shipped-asset count for the 8 new docs (milestone-merge.md + 7
      ms-*.md), the Bootstrap delivery table gaining M018's own row, and the
      Revised Roadmap header/entry reflecting M018 delivered and M019. Also
      reapply and commit the developer's own pre-existing
      docs/assets/workflow.mmd/.svg edit (a Backlog node plus an 'Executing
      Tasks' subgraph label, stashed before this milestone's confirmation so its
      baseline commit could proceed on a clean tree) -- folded into this task's
      write_scope and single completion commit rather than a separate standalone
      commit, per explicit developer decision at confirm time (AC018, AC019,
      AC020, AC021)."
    status: waiting
    depends_on:
      - T002
      - T003
      - T004
      - T005
    acceptance_criteria:
      - "pitway backlog show B001/B002/B003 each read status: promoted, with
        promoted_to correctly naming M019 and T003/T004/T005 respectively"
      - pitway backlog list --status pending shows only B004 remaining
      - IMPLEMENTATION_PLAN.md's command-surface count reads 25
      - IMPLEMENTATION_PLAN.md's shipped-asset count reflects the 8 new docs
      - The Bootstrap delivery table includes M018's row
      - The Revised Roadmap header/entry reflects M018 delivered and names M019
      - Any other discovered drift between the document and this milestone's
        actual delivery is corrected in this task, not left stale
      - docs/assets/workflow.mmd/.svg carry the stashed Backlog-node/
        Executing-Tasks-label edit, committed in this task's own commit
    write_scope:
      - IMPLEMENTATION_PLAN.md
      - docs/assets/workflow.mmd
      - docs/assets/workflow.svg
    verification:
      strategy: manual
      detail: developer/driver review per CT012/CT013's instructions, plus a full npm
        test run (CT014) confirming no regression across the whole suite
    result: null
    usage: null
  - id: T007
    name: Driver runtime usage-propagation contract
    objective: "Establish a driver-agnostic, MUST-level integration requirement:
      when a dispatched worker/subagent returns runtime-reported usage (an
      Agent-tool-style dispatch result carrying token counts), the driver MUST
      extract it and pass it to task-update <id> completed --usage <json>. A
      dedicated pre-task investigation confirmed the usage-persistence pipeline
      (taskUsageSchema, the --usage CLI flag, parseUsageInput, accumulateUsage,
      aggregateUsage) is already correct -- this task changes NONE of it. The
      gap is that no driver-facing document ever instructed the
      extraction/propagation step. Update IMPLEMENTATION_PLAN.md section 8
      (Agent Interface) / section 12 (Token Accounting Strategy) with the
      provider-agnostic statement of this requirement (so a future driver
      adapter, e.g. a hypothetical OpenCode adapter, inherits the same
      obligation), and update src/integrations/claude/dispatch.md's
      worktree-dispatch completion step plus
      src/integrations/claude/protocol-driver.md with the concrete Claude
      instruction, cross-referenced from
      src/integrations/claude/commands/task-update.md. Explicitly preserve: when
      no task-scoped runtime usage is available (fully inline execution, or a
      worker reporting none), usage MUST remain null/N/A -- never estimated,
      fabricated, or defaulted to zero. This task is inserted at the top of
      M019's task graph with depends_on: [] (task-add/task-amend have no
      sanctioned mechanism to retroactively gate already-ready T001/T004/T005 on
      a newly-added task -- disclosed in the contract's Design Decisions); its
      priority is a driver-discipline instruction: complete this task before
      dispatching any other M019 task (AC022, AC023)."
    status: completed
    depends_on: []
    acceptance_criteria:
      - IMPLEMENTATION_PLAN.md section 8/section 12 state the usage-propagation
        requirement as a provider-agnostic driver obligation, not
        Claude-specific prose only
      - src/integrations/claude/dispatch.md's worktree-dispatch completion step
        explicitly instructs the driver to extract a dispatched worker's
        reported usage and pass it via task-update completed --usage <json>
      - src/integrations/claude/protocol-driver.md states the same MUST-level
        rule; src/integrations/claude/commands/task-update.md cross-references
        it
      - No change is made to taskUsageSchema, the --usage CLI flag,
        parseUsageInput, accumulateUsage, or aggregateUsage
      - A test proves a real --usage value supplied at the worktree-dispatch
        completion path (task-dispatch -> work -> task-integrate -> task-update
        completed --usage) persists correctly end-to-end
      - A test proves usage remains null when no --usage is supplied at
        completion, including for a fully inline (non-dispatched) completion
      - A test proves accumulateUsage/parseUsageInput never coerce a
        missing/absent usage field to 0 or otherwise estimate it
      - A documentation-presence test asserts dispatch.md/protocol-driver.md
        literally contain the MUST-level --usage propagation instruction
    write_scope:
      - IMPLEMENTATION_PLAN.md
      - src/integrations/claude/dispatch.md
      - src/integrations/claude/protocol-driver.md
      - src/integrations/claude/commands/task-update.md
      - tests/integration/task-update.test.ts
      - tests/unit/claude-assets.test.ts
    verification:
      strategy: tdd
      detail: npm test -- tests/integration/task-update.test.ts
        tests/unit/claude-assets.test.ts
    result:
      summary: "[truncated] ss a dispatched | worker's runtime usage via task-update
        completed --usage. Updated | IMPLEMENTATION_PLAN.md, dispatch.md,
        protocol-driver.md, task-update.md. | No usage schema/aggregation
        change. Regression reqs 1-3 already covered | by pre-existing tests;
        added a new doc-presence test."
      evidence: Tests  67 passed (67) | Start at  23:00:16 | Duration  44.94s
        (transform 305ms, setup 0ms, import 413ms, tests 44.51s, environment
        0ms)
    attempts: 1
    usage: null
