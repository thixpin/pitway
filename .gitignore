node_modules/
dist/
coverage/
.DS_Store
/reports/
drafts/

# `pitway init`-installed output, not source. The real source lives at
# src/integrations/claude/ (tracked) and is installed verbatim into a
# managed repo's own .claude/ (never this one). /CLAUDE.md is hand-authored
# and already tracked -- deliberately not listed here.
/.claude/
/AGENTS.md
