node_modules/
dist/
coverage/
.DS_Store
/reports/
drafts/

# `pitway init`-installed output, not source. The real source lives at
# src/integrations/claude/ (tracked) and is installed verbatim into a
# managed repo's own .claude/ (never this one). /CLAUDE.md and /AGENTS.md
# are hand-authored and already tracked -- deliberately not listed here.
# /.agents/ is the Codex harness artifact, recreated on every Codex
# session in this repo.
/.claude/
/.opencode/
/.agents/

# Machine-local OpenCode configuration (developer's own, never tracked)
opencode.json
