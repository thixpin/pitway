# PitWay Implementation Plan (§56)

Status: **approved by the developer on 2026-08-18**, with all five §17 open questions resolved (answers recorded inline in §17). Implementation proceeds via the bootstrap milestone map below, starting with the M001 contract.
Revised **2026-08-19** after M004's completion: §7 reflects the actual 11-command surface, §10 records the discovered trailer-lookup limitations, §16 adds risk 8 (task-amendment gap), and the Bootstrap section records the actual M001–M004 outcomes plus the revised M005–M011 roadmap. Revised again **2026-08-19** (M005/T009, then M006/T006): §7 now reflects the actual 14-command surface through M006 (`task-amend`, `write-ms-artifacts`, `auto-run` added), §9 reflects M006's actually-delivered Claude Code Adapter (full asset list, driver/worker split, interactive UX, bootstrap disclosure), §11 records M006's verification-execution hardening, and the Bootstrap section's delivery table now includes M005. Revised again **2026-08-19** (M007/T013): §7 confirms the command surface stayed at fourteen through M007 (a deferred fifteenth, `quick-change`, is design-only); §8 records the context-bundle duplication defect fixed for real (`mapped_ac_ids`) and discloses read-boundary enforcement as still genuinely undecided; §9 records M007's protocol-hardening additions (conflict-preflight, Decision Authority Policy, dispatch-mode rule); the Bootstrap section's delivery table now includes M006; the Revised Roadmap's M007 entry and Status line reflect M007's actual delivery and the Adaptive Workflow Intensity decision. Revised again **2026-08-19** (M008/T005): §7 confirms the command surface stayed at fourteen through M008 (no new command — the real `dist/`-built, npm-packaged binary and its symlink-safe entry point are new delivery, not new surface); §8's read-boundary-enforcement question is resolved (advisory-only, permanent-until-revisited — see M008's roadmap entry); the Bootstrap section's delivery table now includes M007 (M008's own row is correctly omitted here, per the same self-referential discipline, left for M009's own reconciliation task); the Revised Roadmap's M008 entry reflects its actual delivery; the Status line is restructured to point at live `pitway resume`/`.pitway/state.yaml` rather than a prose snapshot, closing a staleness pattern that had repeated across M005, M006, and M007's own equivalent tasks. Revised again **2026-08-20** (M009/T006): §7 reflects the actual seventeen-command surface delivered through M009 (`milestone-cancel`, `verification-repair`, `quick-change` added; `milestone-add` gains `--replace`); §9's Claude-asset count moves to 23 files (three new command docs plus a `protocol-driver.md` addition); the Bootstrap section's delivery table now includes M008 (M009's own row is correctly omitted here too, left for M010's own reconciliation task); the Revised Roadmap gains M009's actual-delivery entry plus two new placeholder milestones — M010 (Worker Verification Evidence) and M011 (Claude Skills, shifted from its earlier M010 slot) — and one unnumbered, time-boxed Token Telemetry Spike candidate positioned after M011; the milestones this document previously numbered M009/M010/M011 (branch isolation, parallel worktrees, extended dogfood) are renumbered M012/M013/M014 throughout, including §10's and §15's own cross-references; the Status line reflects M009's actual delivery. Revised again **2026-08-20** (M010/T004): §7 reflects the actual eighteen-command surface delivered through M010 (`task-verify` added; `task-update` gains `--evidence`); §9's Claude-asset count moves to 24 files (one new command doc plus targeted `protocol-driver.md`/`dispatch.md` insertions); the Bootstrap section's delivery table now includes M009 (M010's own row is correctly omitted here too, left for whichever milestone next runs its own reconciliation task); the Revised Roadmap's M010 entry reflects its actual delivery in place of its earlier not-yet-drafted placeholder; the Status line reflects M010's actual delivery. Revised again **2026-08-20** (M011/T008): §7 confirms the command surface stayed at eighteen through M011 (no new command — `required_skills` is a schema/gate addition surfaced through the existing `task-status --context`); §7's `init` description now covers the corrected per-file collision/idempotency semantics, the new root `AGENTS.md`/`CLAUDE.md` bootstrap-file behavior, and the content-aware git-safety integration covering `quick-change` and baseline staging; §9's Claude-asset count moves to 31 files (six vendored skills plus `NOTICE.md`); the Bootstrap section's delivery table now includes M010 (M011's own row is correctly omitted here too, left for whichever milestone next runs its own reconciliation task); the Revised Roadmap's M011 entry reflects its actual delivery, including the real dogfood-evidence finding and its disclosed non-bootstrap gap, in place of its earlier not-yet-drafted placeholder; the Status line reflects M011's actual delivery. Revised again **2026-08-20** (M012/T009): §7 confirms the command surface stayed at eighteen through M012 (no new command — `branch_strategy` is a config/schema addition, not a CLI surface change); §9's Claude-asset count stays at 31 files (a single targeted `protocol-driver.md` insertion, not a new asset); §10 is rewritten to describe the real, delivered `branch_strategy: main | milestone` behavior in place of the former single-branch-only text, and its range-bounded-trailer-lookup sentence now reflects actual delivery; §15's "Deferred: milestone-level Git branch strategy" subsection is marked delivered, pointing at §10 rather than restating the design; the Bootstrap section's delivery table now includes M011 (M012's own row is correctly omitted here too, left for whichever milestone next runs its own reconciliation task); the Revised Roadmap's M012 entry reflects its actual delivery in place of its earlier not-yet-drafted placeholder; the roadmap-numbering collision flagged when M012 was drafted is resolved — **PitWay Driver UX** is inserted as the new **M013** immediately after M012, and the milestones this document previously numbered M013/M014 (parallel task worktrees, extended dogfood validation) are renumbered **M014**/**M015** throughout, including §10's and §15's own cross-references; a new **Backlog / Pending Candidates** section (per explicit developer request) records four previously session-local-only unscheduled candidates plus M011's two unfixed dogfood structural findings, durably in this document rather than only in session notes; the Status line reflects M012's actual delivery. Revised again **2026-08-20** (M013/T008): §7's developer-facing command list and the `verify` table row note the new `--status`/`--report` read-only flags, and a new `milestone-status` table row documents `--report`; the command-surface sentence confirms eighteen through M013 (no new command); §9's Claude-asset count stays at 31 files (M013's `protocol-driver.md` insertion is a targeted content change, not a new asset), gaining a new "M013 additions" bullet describing the "Progress reporting" section; the Bootstrap section's delivery table now includes M012 (M013's own row is correctly omitted here too, left for whichever milestone next runs its own reconciliation task); the Revised Roadmap's M013 entry reflects its actual delivery, including the disclosed driver_overhead-omission deviation from the original candidate's wording, in place of its earlier not-yet-drafted placeholder; the Backlog section's task-name-field entry is marked delivered, and a new entry records the missing-`.pitway/`-state diagnostic evaluated and deferred during this milestone's own drafting; the Status line reflects M013's actual delivery. Revised again **2026-08-20** (M014/T011): §7 reflects the actual twenty-one-command surface delivered through M014 (`task-dispatch`, `task-integrate`, `task-discard` added — the parallel-worktree lifecycle commands, active only under the new opt-in `execution.strategy: parallel_worktrees` config); §9's Claude-asset count moves to 34 files (three new command docs plus targeted insertions into `protocol-driver.md`, `coordination.md`, `dispatch.md`, and `protocol-worker.md`); §10's former 'no worktrees … sequential execution' line is rewritten to its delivered form — sequential remains the default and byte-identical, `parallel_worktrees` is the delivered opt-in, stashes and automatic merges are still never used — and the per-task-branch principle is reworded to its precise delivered form: tasks never own PERSISTENT branches (the `pitway/task/*` scaffolding branch exists only between dispatch and integrate/discard, never enters mainline history — diff-apply, never merge — and is always deleted with its worktree), resolving an internal plan contradiction in the roadmap entry's own favor (the entry had sanctioned 'one temporary worktree + task branch per dispatched task' against §10's absolute wording); the one-atomic-commit-per-task invariant is preserved byte-for-byte under both strategies, with one disclosed precedented nuance (a completion commit may carry a concurrently dispatched sibling's in_progress state rider in tasks.yaml, exactly as dependent-promotion riders already ride today); the worker-verification refinement is named as deviation (4) from the roadmap entry's literal wording — the entry said each worker 'returns a verified result + commit SHA', the delivery makes worker-side checks advisory and the post-integration main-tree `task-verify` authoritative, because worktree-written evidence resolves to a per-worktree journal and can never reach the authoritative record, and only a post-integration run observes the landed state; the Bootstrap section's delivery table now includes M013 (M014's own row is correctly omitted here too, left for whichever milestone next runs its own reconciliation task); the Revised Roadmap's M014 entry reflects its actual delivery in place of its placeholder; the Status line reflects M014's actual delivery. §§1–13 are otherwise preserved as the approved design record; M001–M008 history is not rewritten. Revised again **2026-08-21** (M015/T011): §7 reflects the actual twenty-two-command surface delivered through M015 (`milestone-review` added — the role-based review-workflow command, the first surface growth since M014 — plus a new command-table row); §9's Claude-asset count moves to 35 files (one new command doc plus a targeted `protocol-driver.md` insertion), gaining a new "M015 additions" bullet; the Bootstrap section's delivery table now includes M014 (M015's own row is correctly omitted here too, per the same self-referential discipline, left for M016's own reconciliation task); the Revised Roadmap header and its opening sequencing paragraph move from "M013 delivered" to "M014 delivered", with M015 now proposed-in-place-of-still-proposed per that same discipline; a new M015 roadmap entry reflects its actual delivery, naming every disclosed deviation/decision (command-shape spacing, known-roles-only MVP, readline-not-arrow-keys, confirm/review uncoupling) plus the bootstrap disclosure that M015's own pre-confirmation review necessarily used the informal M014-style two-reviewer flow rather than the `milestone-review` command it delivers, and the one genuine gate-caught defect corrected via the established M012/M013/M014 widening precedent (`content_hash`'s contract-side component, initially raw file bytes, corrected to a content-only projection); the previously-planned "Extended dogfood validation + release increment" milestone is renumbered from **M015** to **M016** throughout, including the sequencing paragraph's own "renumbered M014–M015" note (now "M014–M016") and its "M015 gates the parallelism increment" close (now "M016"), the Token-Telemetry "never shifts the M013/M014/M015 renumbering" note (now "M013/M014/M015/M016"), and the M014 roadmap entry's own "M015's validation pass" first-real-dispatch pointer (now "M016's validation pass"); the Status line reflects M015's actual delivery. §§1–13 are otherwise preserved as the approved design record; M001–M008 history is not rewritten. Revised again **2026-08-21** (M016/T005): the Bootstrap section's delivery table now includes M015 (M016's own row is correctly omitted here too, per the same self-referential discipline, left for M017's own reconciliation task); the Revised Roadmap header and its opening sequencing paragraph move from "M014 delivered" to "M015 delivered"; the M016 roadmap entry itself is rewritten from its inherited placeholder text to its actual delivery (the first real parallel-worktree dispatch and the first real `git.branch_strategy: milestone` dogfooding in this repository's own history, both proven for real rather than only by synthetic test; the `QuickChangeError`/`requireQuickChange` M011 findings fixed; the three-milestone-recurring nested-full-suite flake root-caused for real — Vitest's default timeout too tight under genuine concurrent-subprocess load, never a PitWay defect — and hardened; context-efficiency evidence recorded, confirming the M007/AC011 bundle-filtering fix works on live data; a real, previously-undiscovered gap found live — `milestone-confirm`'s baseline commit never calls `reconcilePending`, so journal-pending entries whose target file it already committed correctly can block `task-dispatch` indefinitely — discovered attempting this milestone's own first dispatch, worked around procedurally rather than fixed in `src/`, and left as a fresh, disclosed backlog candidate rather than folded silently into this milestone's own scope); the Backlog section marks the M011-findings entry DELIVERED and gains two new real entries (the write_scope/stale-test-assertion gap pattern's fourth recurrence, and the newly-found `milestone-confirm`/`reconcilePending` gap); the "As of this snapshot" narrative paragraph is brought current through M015 and M016 rather than stopping at M014; the Status line reflects M016's actual delivery. Revised again **2026-08-21** (post-M016 merge reconciliation, a developer-requested `quick-change` on `main` rather than a milestone task, since M016 had already completed on its own branch before merging): the Bootstrap section's delivery table now includes M016's own row (its completion and merge into `main` having happened after M016/T005 ran); the M016 roadmap entry's self-contradictory wording about the first real `milestone-review` run is corrected (it had described the QA-role review as not a QA review); the Backlog section gains the load-sensitive-integration-tests item M016/T001's worker disclosed but M016/T005 omitted; the Status snapshot reflects M016 completed and merged (`3ddff06`) plus the three `quick-change` commits landed on `main` between M016's branch point and its merge. No roadmap priorities changed; no backlog item implemented. Revised again **2026-08-21** (M017/T009): §7 reflects the actual twenty-three-command surface delivered through M017 (`task-add` added — inserts one new task into a `confirmed`/`in_progress` milestone, the sanctioned insertion path the Backlog's post-confirm gap had left open); §9's Claude-asset count moves to 36 files (one new command doc, `commands/task-add.md`), gaining a new "M017 additions" bullet; the Bootstrap section's delivery table already includes M016 (added by the post-M016-merge `quick-change`, not by M017); M017's own row stays correctly omitted, per the same self-referential discipline, left for whichever milestone next runs its own reconciliation task; the Revised Roadmap header and its opening sequencing paragraph — left unmoved by the post-M016-merge `quick-change`, which touched only the Bootstrap table and Backlog — now catch up from "M015 delivered" to "M016 delivered", and the structured-failure-evidence-extraction sentence is removed now that T004 delivers it for real; a new M017 roadmap entry reflects its actual delivery, naming every disclosed decision (`task-add` as the sanctioned insertion mechanism, with `milestone-cancel` staying deliberately draft-only rather than gaining a confirmed-milestone cancellation path; the completion-time usage warning chosen over a new `usage-correct` command; a global `vitest.config.ts` `testTimeout`/`hookTimeout` in place of per-test overrides; a new "Drafting write_scope" checklist rule); the Backlog section marks all eight of its still-open, milestone-unassigned items DELIVERED (the `reconcilePending` gap, the task-insertion gap, the missing-state diagnostic, structured failure-evidence extraction, completed-task usage correction, Dependabot configuration, the write_scope/stale-test-assertion pattern's fourth recurrence, and the load-sensitive integration tests) with the chosen direction recorded for each — the load-sensitive item's `npm-pack.test.ts` transient explicitly left open, since it was never a timeout-signature failure and the global timeout increase does not claim to fix it; the Status snapshot reflects M017 completed. Revised again **2026-08-21** (M018/T006): §7 reflects the actual twenty-four-command surface delivered through M018 (`backlog` added — the deferred-work-capture surface, closing the discover → capture → defer → continue → promote gap the now-superseded Backlog / Pending Candidates section existed to work around); §9's Claude-asset count moves to 37 files (one new command doc, `commands/backlog.md`), gaining a new "M018 additions" bullet; the Bootstrap section's delivery table gains M017's own row (correctly omitted by M017/T009 itself, per the same self-referential discipline, added here instead); M018's own row stays correctly omitted, left for whichever milestone next runs its own reconciliation task; the Revised Roadmap header and its opening sequencing paragraph catch up from "M016 delivered" to "M017 delivered", and gain a new M018 roadmap entry reflecting its actual delivery, including the pre-implementation `milestone-review` finding that closed a real safety gap (unconditional active-milestone journal attachment, no override) before any code was written; the Backlog / Pending Candidates section gains an explicit redirect note — `pitway backlog add`/`.pitway/backlog.yaml`/`pitway backlog list` are now the live, authoritative source, its own existing entries preserved as frozen history, never migrated or deleted; the Status snapshot reflects M017 and M018 both completed, naming M018's own two `task-amend` write_scope corrections and its four real, non-fabricated dogfooded backlog items (B001–B004). Revised again **2026-08-22** (M019/T006): §7 reflects the actual twenty-five-command surface delivered through M019 (`milestone-merge` added — the manual-merge gap disclosed since M012, closed for real), and a new note names the 7 `ms-*` command aliases added to the 7 named milestone-* commands plus the `PitWay: ` description prefix on every owned command doc; §8 gains the driver-agnostic runtime usage-propagation MUST-requirement (T007); §9's Claude-asset count moves to 45 files (one new command doc, `commands/milestone-merge.md`, plus 7 `commands/ms-*.md` alias docs), gaining a new "M019 additions" bullet; §12/§16 are revised to reflect the usage-propagation gap as closed at the driver-protocol level, not merely disclosed; the Bootstrap section's delivery table gains M018's own row (correctly omitted by M018/T006 itself, per the same self-referential discipline, added here instead); M019's own row is intentionally left for whichever milestone next runs its own reconciliation task; the Revised Roadmap header and its opening sequencing paragraph catch up from "M017 delivered" to "M018 delivered", and gain a new M019 roadmap entry reflecting its actual delivery, including the pre-confirm `milestone-review` (architect + devops) findings closed before any code was written and every mid-execution incident disclosed rather than smoothed over (an unfinished dispatched-worker turn resumed rather than trusted, a `write_scope` drafting bug of the same class M018/T005 hit, a transient resource-contention verification hang confirmed not a code defect); the Status snapshot reflects M017, M018, and M019 all completed, naming M019's own real findings and its promotion of three of M018's own genuinely dogfooded backlog items (B001–B003) into planned work — the first real exercise of `backlog promote`. Revised again **2026-08-22** (M020/T006, closing the reconciliation gap M019/T006 itself left open per its own self-referential-omission discipline): the Bootstrap section's delivery table gains M019's own row (correctly omitted by M019/T006 itself, added here instead), inserted after M018's row rather than before it (a drafting slip in this same reconciliation, caught and fixed before commit); the Revised Roadmap header catches up from "M018 delivered" to "M019 delivered" (M020 onward now the proposed tail); §7's stale "`milestone-merge` deliberately has no `ms-merge` alias" note is corrected — a post-M019 `quick-change` (`qc-7e6fb2a4`) closed that gap for real, 7 aliases becoming 8; §9's Claude-asset count moves from 45 to 46 files for the same reason (`commands/ms-merge.md`), gaining a new bullet documenting the quick-change as an asset-count-affecting event distinct from a milestone delivery; the Status snapshot now names both post-M019 `quick-change` commits landed on `main` (`cbe1a15` the alias fix, `3a297d9` the B005 report-table-format fix) and that M020 (CLI Branch Coverage Hardening — B004, prioritized milestone-review > task-verify > task-integrate > verification-repair > cli/index, real branch-coverage baselines and targets per file, replacing the ambiguous line-vs-branch B004 wording) is `in_progress`, this reconciliation task being part of its own graph; `pitway backlog promote B004 --task T001` and `pitway backlog archive B005` both run as part of this same task, alongside a new backlog item recording the archive-requires-active-milestone limitation as a twice-recurred dogfood finding (this document's Backlog section stays a frozen historical record per M018/T006 and is not itself edited for `.pitway/backlog.yaml`-tracked items — B006, added mid-milestone via a direct developer request unrelated to B004/B005, separately records a token-usage/cost-tracking gap in `milestone-review` itself). Revised again **2026-08-22** (M021/T003): §4 gains a note on `reviews.yaml`'s new nullable `usage` field and the runtime journal's new milestone-less `backlog_archive` sibling kind (neither file was ever shown in §4's own directory sketch, which predates both `reviews.yaml`/M015 and the journal/M005 — noted rather than silently left unexplained); §7's `milestone-review` row documents the new `record --usage`/`report` usage-disclosure behavior (tokens only, cost explicitly out of scope per decision 8 — no dollar figure exists anywhere in the runtime to derive one from); §7's `backlog` row documents that `archive` alone no longer requires an active milestone, `add`/`list`/`show`/`promote` completely unchanged. `pitway backlog promote B006 --task T001` and `B007 --task T002` both close out the backlog items formally. No Bootstrap-table/Revised-Roadmap-header changes this pass — M021's own row is left for whichever milestone next runs its own reconciliation task, per the established self-referential-omission discipline.
Inputs: the original PitWay specification (a 57-section design document, removed from the repository at the developer's request after approval — inline "spec §" citations below refer to it; its binding content is fully incorporated into this plan and CLAUDE.md) + the 15 confirmed architectural decisions of 2026-08-18 (Claude-driven model, versioned `.pitway/`, commit trailers, no per-task percentages, verification schema, frontmatter contracts, retry-accumulating usage, schema versioning, git required, task state machine, changelog traceability, dogfooding, two-phase bootstrap, preserved decisions). Those decisions are treated as fixed; this plan builds on them and does not reopen them.

---

## 1. Proposed Architecture

```text
Developer ⇄ Claude Code (driver: conversation, reasoning, agent execution)
                │  slash commands + driver protocol
                ▼
        PitWay CLI  ←──────────── also usable directly by the developer
                │  (the machine API: every read and every state mutation)
                ▼
        PitWay Core (milestones, contracts, tasks, dependencies,
                     verification, git, metrics — pure use cases)
                ▼
        State Store (.pitway/ read/write + schema validation)
                +
        Git module (child_process → git)
```

**Core rule: the driver never touches `.pitway/` directly.** Every state mutation and every state read goes through a CLI command; Core validates every transition. *Why:* this is the only boundary that keeps Core agent-agnostic in a Claude-driven world — Claude converses and reasons freely, but the workflow state can only change through validated use cases. A future Codex/Gemini/OpenCode integration is "teach that agent to call the same CLI," with zero Core changes. It also means the CLI works identically when a human types the commands (bootstrap Phase 1, debugging, non-agent use).

Core is a library with no I/O of its own except through the State Store and Git module; the CLI layer is argument parsing + output formatting only. Claude-specific material lives entirely in `src/integrations/claude/` as installable text assets (see §9) — no Claude imports anywhere in Core, satisfying spec §2/§52.

## 2. Repository Structure

```text
pitway/
├── package.json              # bin: pitway
├── src/
│   ├── cli/                  # command definitions, --json/human formatting
│   ├── core/                 # milestones/ contracts/ tasks/ verification/ git/ metrics/
│   ├── state/                # .pitway/ schemas (v1), load/save, validation
│   └── integrations/claude/  # slash-command templates + driver protocol (text assets)
├── templates/                # files written by `pitway init`
├── tests/
│   ├── unit/
│   ├── integration/
│   └── workflow/
├── .pitway/                  # PitWay's own dogfooded state (decision 13)
└── IMPLEMENTATION_PLAN.md
```

**Proposed stack** (open question #1, recommendation): TypeScript (strict) on Node ≥ 20, ESM. Runtime dependencies limited to `commander` (CLI parsing), `yaml` (state files), `zod` (schema validation — correctness is priority 1 and hand-rolled validators are where state stores rot). Dev: `vitest` + `tsup` (or `tsc`) build. *Why TS:* the domain is state machines and schemas; the type system is cheap insurance exactly there. *Why so few deps:* spec §57.

## 3. Core Domain Model

Entities: **Requirement** (optional BRS doc), **Milestone** (owns a Contract), **Contract** (objective, scope, non-goals, acceptance criteria, verification checks, change log), **Task** (node in dependency graph), **VerificationCheck** (CT; `command | manual | review`), **UsageRecord** (measured tokens, categorized).

Use cases (Core's public surface, mirrored 1:1 by CLI commands): `createMilestone`, `confirmMilestone` (incl. amendment re-approval), `transitionTask`, `completeTask` (result + commit), `resolveReadyTasks` (dependency resolution), `buildTaskContext`, `recordVerification`, `runCommandChecks`, `recordUsage`, `resumeSummary`, `statusSummary`. Contract-vs-task separation per spec §10: the contract states what must be true; tasks state how.

## 4. State Schema (`.pitway/`, schema v1)

```text
.pitway/
├── config.yaml               # schema_version: 1 (reserved; no speculative settings)
├── state.yaml                # schema_version, milestones: [M001, …], active_milestone
├── requirements/R001.md      # optional, concise BRS style
└── milestones/M001/
    ├── contract.md           # AUTHORITATIVE contract (frontmatter + body)
    ├── tasks.yaml            # task graph + results + usage
    ├── verification-results.yaml
    └── usage.yaml            # milestone-level measured usage (planning / qa)
```

`contract.md` (decision 7, refined): frontmatter is the machine-authoritative part — `schema_version, id, title, status, requirement, confirmed_at, acceptance_criteria: [{id, text}], verification: [{id, criterion, type, command|instruction}], verification_approved_hash`. The markdown body holds Objective / Scope / Non-Goals / notes / **Change Log** (append-only, decision 12). *Why ACs and CTs in frontmatter rather than body:* Core must enumerate them reliably for AC→CT mapping and `verify`; parsing prose headings is fragile. The body never restates AC/CT definitions — status output renders them from frontmatter, preserving single-source-of-truth.

`tasks.yaml` per task: `id, objective, status, depends_on, acceptance_criteria, relevant_files, verification (strategy + detail), result (null | {summary, evidence}), usage (null | {attempts, total_tokens, input_tokens?, output_tokens?})`. Results are concise structured summaries — never transcripts (spec §7/§20).

`verification-results.yaml`: `[{check, status: pass|fail, at, evidence, recorded_by: command|developer}]`. *Why separate from the contract:* definitions (approved, changelog-controlled) and results (mutable, per-run) are different facts; splitting them is what keeps the contract single-source without freezing results.

Authority rules: milestone status lives only in contract frontmatter; `state.yaml` holds only the index and active pointer (list/status commands read per-milestone files — cheap at this scale, and no dual truth). **No derived git data is persisted** (decision 4): commit SHAs are resolved from trailers on demand. Every file carries `schema_version: 1` (decision 9); loaders reject unknown versions; no migration framework until a v2 exists.

**M021**: a review findings snapshot (`reviews.yaml`, delivered M015 — not shown in the tree above, which predates it) gains a nullable `usage: taskUsageSchema` field, reusing the task-level usage type verbatim (§7's `milestone-review record --usage`). The git-invisible runtime journal (delivered M005 — also not shown above) gains a tenth sibling record kind, `backlog_archive` (`id`, `target`, `reason`, `at`, deliberately no `milestone` field, mirroring `quick_change`'s own milestone-less precedent) — never checkpoint-eligible, structurally excluded from pending-amendment resolution.

## 5. Milestone State Machine

```text
draft → confirmed → in_progress → review → completed
draft | confirmed → cancelled
review → in_progress          (contract verification failed → local re-plan, spec §29)
```

`confirmed → in_progress` happens at the baseline commit. `review` is the contract-verification + Senior-QA boundary. Six states, no micro-states.

## 6. Task State Machine (decision 11, adopted verbatim)

```text
planned → waiting → ready → in_progress → review → completed
in_progress → blocked;  blocked → ready
in_progress → failed;   failed → ready
planned | waiting | ready → cancelled
```

Semantics: tasks are `planned` while the milestone is a draft; milestone confirmation moves all to `waiting`; dependency resolution promotes to `ready` (no deps ⇒ ready immediately). Core rejects invalid transitions with an explicit error naming the allowed ones. Retries: `failed → ready` re-enters the same task; usage accumulates (§12 below).

## 7. CLI Commands

All commands support `--json` (the machine interface Claude consumes) alongside human-readable output with the racing presentation layer (spec §1).

Developer-facing (spec §37, unchanged): `init`, `milestone-add`, `milestone-status`, `milestone-list`, `task-status`, `resume`, `verify`. **M013:** `verify <id> --status` and `milestone-status <id> --report` are two new, purely read-only flags on existing commands (§ below), not new commands — `resume` and `milestone-status` also gain a `footer: string | null` field (the permanent one-line racing progress footer, non-null only once a milestone is confirmed).

Machine-facing additions — each is a required use case in the Claude-driven model, not speculation:

| Command | Purpose |
|---|---|
| `milestone-add --contract <file> [--tasks <file>] [--requirement <file>]` | Create milestone in `draft` from driver-drafted artifacts (validated against schema) |
| `milestone-confirm <id> [--amend --file <path>]` | Human gate: freeze contract, hash-approve verification commands, git safety check, baseline commit, tasks → `waiting`/`ready`. `--amend` (M005/T004) takes a validated draft contract file — never a hand-edit of the live `contract.md` — journal-records and materializes immediately, no commit of its own; folds into the next checkpoint. **M012:** under `git.branch_strategy: milestone`, a fresh confirm also creates the milestone's deterministically-named branch and records `base_branch`/`base_revision`; a resumed confirm performs no branch mutation and refuses on any position mismatch (§10) |
| `task-status <id> [--context]` | `--context` emits the minimal task-context bundle (§8) |
| `task-update <id> --status <s> [--result <file>] [--usage <json>] [--message <file>] [--evidence <id>]` | Validated transitions; `--status completed` performs the atomic task commit, optionally resolving a `task-verify` evidence record (implicit newest-by-append-order for the milestone+task, or the named `--evidence <id>`) as the authoritative `result.evidence` (M010/T002) |
| `task-verify <id> [--typecheck <command>]` | M010/T001-T003: executes an `in_progress` task's own approved `command`/`tdd` verification command (reusing the existing `executeCommand` primitive, never a new subprocess mechanism) plus an optional typecheck, and persists a formal, resumable, git-invisible `task_verify_evidence` journal record — a fingerprinted (declared `write_scope`/`relevant_files` only, never `.pitway/` state), resumable replacement for an ad hoc independent rerun-and-eyeball |
| `task-dispatch <id>` | M014/T004: prepares one parallel-eligible task under `execution.strategy: parallel_worktrees` (refuses under `sequential`): eligibility against every `in_progress` task (dependency-independence + pairwise-disjoint `write_scope`; legacy `relevant_files` tasks never eligible), dirty-tree and journal-pending-amendment refusals, then `ready → in_progress`, a `worktree_dispatch` journal record, and a temporary worktree on scaffolding branch `pitway/task/<mId>-<tId>`. `--json` is the worker handoff envelope only — the driver passes the context bundle from the main root |
| `task-integrate <id>` | M014/T006-T007: diff-apply integration, one task at a time — combined range diff (`--binary --no-renames`) validated against `write_scope` (deletions included, `.pitway/` always refused), pre-checked apply landing uncommitted in the main tree, `worktree_integrate` journal record (worker SHA evidence-only), worktree/branch cleanup. Never a merge; completion stays the existing task-verify → review → completed path. Idempotent re-runs across both crash windows (cleanup-only; applied-but-unrecorded via reverse-apply detection) |
| `task-discard <id> --reason <text>` | M014/T008: the abandonment exit for a live dispatch — `worktree_discard` journal record, cleanup (tolerating a vanished worktree), `in_progress → failed`; `failed → ready` re-allows dispatch. Direct `task-update` status changes on a live-dispatched task refuse, pointing at the two exits; `pitway resume` classifies every crash residue read-only |
| `usage-add <milestone> --category planning\|qa --usage <json>` | Milestone-level measured usage (decision 8); M005/T004: journal-records and materializes `usage.yaml` immediately, no commit of its own — folds into the next checkpoint the same way |
| `verify [<id>] [--check CTnnn] [--pass\|--fail --evidence <text>] [--status]` | Run approved `command` checks; record `manual`/`review` outcomes. **M013:** `--status` renders the latest recorded result per declared check, purely by reading `verification-results.yaml` via a shared helper (`computeLatestCheckResults`, also reused by `milestone-complete`'s own gate and the workload/footer/report functions below) — never executes anything, works for any milestone status |
| `milestone-complete <id>` | Validated `in_progress → review → completed` in one persisted write, gated on every non-cancelled task completed and the latest result per check passing; clears `active_milestone`; resumable completion commit (added during M004 — completion proved to need its own validated use case, symmetric with confirm). **M012:** under a tracked branch, refuses on a checked-out-branch mismatch before committing, and reaches a merge-ready/PR-ready state — never an automatic merge (§10) |
| `task-amend <id> --file <path> --change-log <text>` | M005/T005: validated, developer-approved task-definition amendment (objective/acceptance_criteria/relevant_files/context_files/write_scope/verification only — identity, status, dependencies, and history stay immutable); journal-records and materializes `tasks.yaml` immediately, no commit of its own; cumulative chained amendments for the same task supported (never a silent "latest wins") |
| `write-ms-artifacts --contract <file> --tasks <file> --destination <dir> [--overwrite]` | M005/T006: writes a draft contract + task graph as non-authoritative files to an explicit destination outside `.pitway/`, reusing `milestone-add`'s validation without its state-mutating half — no confirm, no commit, no dispatch |
| `auto-run enable\|disable\|status [milestone-id]` | M006/T005: agent-agnostic auto-run authorization, managed entirely through this command — never written directly by the Claude integration layer. `enable` requires the target milestone be `in_progress` and records its current `verification_approved_hash`; `disable`/`status` work for a milestone in any status. Authorization is derived purely (`isAutoRunAuthorized`) from a new sibling journal record kind (`auto_run` — never checkpoint-eligible, structurally excluded from `derivePending`/`resolveTargetPath`/checkpoint folding, not a 4th value of the existing amendment-type enum) and invalidates on a hash change or a later contract/task amendment, both computed from the journal's own append order. `status` names the specific reason when not authorized. Subcommands nest under one top-level `auto-run` entry, not three separate top-level commands |
| `milestone-add --replace <id>` | M009/T001: draft-only, same-id, no-git-operation in-place correction of a draft milestone's contract/tasks/verification-results/usage/verification-repairs — a flag on the existing command, not a new one. Refuses outside `draft` status, pointing to `milestone-confirm --amend`. Preserves the draft's currently materialized requirement id when `--requirement` is omitted (requirements are append-only, never cleared or deleted) |
| `milestone-cancel <id>` | M009/T001: the separate preserve-and-retire path for a draft milestone — no git operation (nothing is committed for a draft), the directory and `contract.md` are never deleted (preserved with `status: cancelled` as a permanent record), and the id is never reused |
| `verification-repair approve\|commit\|cancel` | M009/T002: a bounded, two-phase, approve-before-edit correction for a milestone whose tasks are all completed but which is not yet `milestone-complete`d. `approve` locks an immutable `VR\d{3}` id, exact file/check scope, and rationale (repeatable `--file`/`--check`, each rejecting empty/duplicate/out-of-repository/non-command-check input) before any implementation edit is permitted. `commit` validates the dirty diff against exactly that scope, reruns every approved check by reusing `pitway verify`'s own execution/recording path (fail-closed — refusing the whole commit on any failure), and atomically commits the VR record, the corrected files, and the fresh verification results under `PitWay-Milestone`/`PitWay-Verification-Repair` trailers. `milestone-complete` refuses while any VR is still pending |
| `milestone-status <id> [--report]` | **M013:** `--report` renders the full structured Progress Report — title, weighted workload % (decision-5-compliant five-band formula), the authoritative completed/total task count, recorded token total + missing-usage count, a task table (id, `name`-or-truncated-objective label, evidence-honest status — `completed · verified` only with a real `task_verify_evidence` journal record, plain `completed` otherwise), the critical path (longest remaining dependency chain), active/next task, a token breakdown limited to the directly-measured task/planning/qa categories (deliberately no `driver_overhead` figure — see §12), and the racing footer as its final line. Default (no `--report`) output is unchanged |
| `quick-change create\|approve\|run\|commit\|cancel\|promote\|status` | M009/T003-T005: a bounded, small-fix workflow against an already-completed milestone, never reopening or rewriting it or any of its existing commits. Requires `active_milestone: null` and a clean working tree at `create`; the declared `--scope` is an exact file census (every path under `.pitway/` protected); `approve` hashes and locks scope + verify command; `run` executes it, recording every attempt append-only; `commit` requires a fresh passing run and lands one commit under a `PitWay-Change` trailer; `cancel` (draft/approved only) and `promote` (draft/approved only, terminal — converts to a milestone-draft candidate via the change's own objective/scope, never fabricating contract content itself) round out the lifecycle. `pitway resume` is the authoritative recovery view for a pending change; `status` is a focused-inspection convenience, never a substitute |
| `milestone-review start\|brief\|record\|report\|decide` | M015/T002-T007: a role-based review workflow against a draft/confirmed/in_progress/review milestone (never completed/cancelled) — PitWay manages review state only (sessions, findings, decisions); the driver dispatches reviewer subagents and reconciles their findings. `start --roles <csv>` (or an interactive `node:readline` numbered multi-select when omitted on a TTY, no new runtime dependency) opens a session, pinning a canonical content-hash projection of the contract's own CONTENT (id/title/requirement/acceptance_criteria/verification + body) plus every task's DEFINITION, with execution/lifecycle fields excluded on both sides; `brief`/`record` refuse on a stale hash (a real revision — `task-amend`/`--replace`/`--amend` — moved the milestone's content mid-review; a task transition or `milestone-confirm`'s own status promotion never does); `report` renders severity-ordered findings, mechanically grouped conflicts/overlaps, and the honesty text (a finding is reviewer opinion-evidence, never proof); `decide --outcome accepted\|revision_requested\|rejected` requires every selected role recorded, unless `rejected` explicitly abandons an unfinished review. Every mutation is journal-backed exactly like a usage recording or amendment; no review command ever writes `contract.md`/`tasks.yaml`. **M021:** `record` gains an optional `--usage <json>` flag (measured only, never estimated, mirroring `task-update`'s own parsing) attached to the recorded role's findings snapshot; `report` renders a per-role usage line and a session-level total, tokens only — no cost figure, since the runtime reports no dollar amount and decision 8 forbids deriving one |
| `task-add <milestone-id> --file <yaml> --change-log <text>` | M017/T002: inserts one new task into a `confirmed`/`in_progress` milestone — the sanctioned mid-flight insertion path, closing the Backlog gap `milestone-cancel`'s draft-only boundary deliberately does not (§ its own row above). Validates the file against the existing `taskSchema` minus execution-only fields (rejected outright if present), requires the next sequential `Tnnn` id and a non-empty `name`, requires every `depends_on` id to name an existing non-cancelled task, and reuses the existing `task_amendment` journal operation unchanged (no schema/operation-type change) — so an open `milestone-review` session stales and an active `auto-run` authorization invalidates for free, exactly as any other task amendment already does. Refuses `draft` (pointing at `milestone-add --replace`), `review` (pointing at `verify`/`milestone-complete`), and `completed`/`cancelled`. The dispatch rule this exists for: the pending entry makes `task-dispatch` refuse until the next checkpoint commit folds it, while inline `task-update <new-id> in_progress` is allowed immediately |
| `backlog add\|list\|show\|promote\|archive` | M018/T001-T003: captures work discovered mid-task that is intentionally out of scope, without expanding the current task or milestone's own scope — the third correction-mechanism option alongside `task-add` (in-scope insertion) and `quick-change` (already-completed-milestone fix). `.pitway/backlog.yaml` (root-level, schema v1, absent-tolerant) is the single authoritative store; every mutation requires an active milestone and attaches its journal entry to it *unconditionally* — no override, by design (`backlog.yaml` is the first journal-backed target not exclusively owned by one milestone's directory, so an override could misattribute a pending entry to a retired milestone that would never checkpoint it — a real gap this milestone's own review caught and closed before implementation). `add`'s `--milestone`/`--task` are `source` annotation only (defaulting `source.milestone` to the active milestone when omitted); `promote <id> --task <id> [--milestone <id>]` means the promotion target only (defaulting `--milestone` to the active one) and never creates the task/milestone itself — the driver runs `task-add`/`milestone-add` first, then `promote` links and closes the item; `archive <id> --reason <text>` takes no milestone/task flag at all. Reuses the existing checkpoint-eligible journal machinery via one new `backlog_recording` operation type (never a new commit primitive, never a dedicated commit) — `classifyDirtyPaths`/`reconcilePending` needed zero changes beyond one `resolveTargetPath` case. `pitway resume` gains a read-only `pendingBacklogItems` view. **M021:** `archive` no longer requires an active milestone (the only subcommand this applies to — `add`/`list`/`show`/`promote` are completely unchanged) — it finalizes an already fully identified existing item rather than creating new pending state, so the misattribution risk the original design guarded against never applied to it; journal-backed via a new milestone-less sibling record kind (`backlog_archive`, §4), not the shared `backlog_recording` entry type |
| `milestone-merge <id> [--target <branch>]` | M019/T001-T002: lands a `completed` milestone's own branch into a target branch (default: its recorded `base_branch`) — the manual-merge gap disclosed since M012, exercised by hand for M016/M018 before this. Every state read (contract frontmatter, `base_branch`, the completion commit SHA) resolves from the *current* branch before any checkout, refusing by name — not via a blanket branch-name assertion — when the completion commit isn't reachable from current HEAD (covers wrong-branch, detached-HEAD, and a `.pitway/` directory absent on this branch entirely, while still allowing the legitimate idempotent re-run from the target branch post-merge). Git safety, all checked before mutation: dirty tree, missing target branch, missing source branch, an already-in-progress merge (`MERGE_HEAD` present, named distinctly from a generic dirty-tree refusal), and a real conflict — every failure restores the original branch, never leaves a half-merged tree. Idempotent via `git merge-base --is-ancestor` against the completion commit *SHA*, not the branch tip, so it stays correct after the source branch is deleted. `--no-ff` merge commit `merge: <id> <title>`, no `PitWay-*` trailer (matching the M016/M018 manual-merge precedent — a merge is not task-scoped work). Recorded as a new sibling journal record kind, `milestone_merge` (audit-trail only, like `worktree_integrate`/`worktree_discard` — never checkpoint-eligible, no `resolveTargetPath` case) |

Twenty-five commands total as implemented through M019 (M015 added `milestone-review`; M016 added none; M017 added `task-add`; M018 added `backlog`; M019 added `milestone-merge`): `init`, `milestone-add`, `milestone-cancel`, `milestone-confirm`, `milestone-list`, `milestone-status`, `milestone-merge`, `milestone-review`, `task-status`, `task-add`, `task-amend`, `task-dispatch`, `task-integrate`, `task-discard`, `task-update`, `task-verify`, `resume`, `verify`, `milestone-complete`, `usage-add`, `write-ms-artifacts`, `auto-run`, `verification-repair`, `quick-change`, `backlog` — all registered on the single entry point (`buildCli` + `registerAllCommands`, M004/T007; extended M005/T005-T006, M006/T005, M009/T001-T005, M010/T003, M014/T004-T008, M015/T002, M017/T002, M018/T003, M019/T002). `quick-change` closes the gap the M007 design-only entry (`docs/evidence/M007/quick-change-design.md`) left open — real code, real CLI surface, no longer deferred. *Why this doesn't violate "small surface":* the surface grows only where the driver needs a mutation Core must validate; every alternative (Claude editing YAML directly) breaks the architecture's one load-bearing rule.

**M019/T003**: the 7 named milestone-* commands (`milestone-add`/`-cancel`/`-complete`/`-confirm`/`-list`/`-review`/`-status`) each gain a native Commander `.alias('ms-<verb>')` — `pitway ms-<verb>` invokes identically, including `milestone-review`'s own subcommands under `ms-review`. Aliases add no new `program.commands` entries (the 25-command count above is unaffected); at M019 confirm time `milestone-merge` deliberately had no `ms-merge` alias, since it postdated the backlog item's fixed, enumerated command list — closed by a post-M019 `quick-change` (`qc-7e6fb2a4`, developer-requested follow-up) adding the 8th alias the same way; no `task-*` command is aliased. Every PitWay-owned command doc's `description:` frontmatter is prefixed `PitWay: ` (the 6 vendored skills excluded — their frontmatter is part of the byte-for-byte vendoring guarantee, §9).

`init` (decision 10): refuses outside a git work tree with a clear message (`git init` instructions, no auto-init); writes `.pitway/` skeleton + Claude integration files (opt-out `--no-claude`, open question #4). **M011/AC002 correction:** the former coarse `.claude/` probe (present/missing/invalid, refusing on any partial mix) is replaced by a per-file, content-comparing classification (`classifyClaudeAssets`) — absent assets install, byte-identical assets are a true no-op, any byte-different conflict refuses the whole command atomically, naming every conflicting path. **M011/AC004:** `init` also creates two new root files, `AGENTS.md` (generic) and `CLAUDE.md` (Claude-specific), non-destructively — created when absent, left alone when byte-identical, preserved untouched with a warning when different/user-authored; `--no-claude` still creates/preserves `AGENTS.md` (not Claude-specific) but skips `CLAUDE.md`. **M011/AC005:** the same content-aware classification now governs `quick-change create`/`commit`'s clean-tree checks and `milestone-confirm`'s baseline staging for both `.claude/` assets and the two root files (via the shared `listSafeManagedDirtyPaths`), closing a real gap that had existed for `.claude/` assets alone since M006.

## 8. Agent Interface

MVP agent interface = **the CLI's `--json` contract** plus one deterministic artifact: the **task context bundle** from `task-status <id> --context`, containing exactly the spec §20 list — task definition, task acceptance criteria, relevant contract excerpt (objective + mapped ACs only), dependency results (their concise `result.summary`), relevant file paths, verification instructions. *Why generated rather than driver-assembled:* context minimization becomes a deterministic Core behavior instead of a prompt-discipline hope; every current and future adapter gets it for free. Accepted limitation (decision 1): the surrounding Claude session context cannot be isolated; PitWay guarantees only that *it* never forwards milestone history or unrelated state into the bundle. M006's dogfood measurement (`docs/evidence/M006-context-efficiency.md`) found this last guarantee was not fully met by the implementation at the time: `contractExcerpt.acceptanceCriteria` returned the *entire* contract's AC array unfiltered, not scoped to a task's own mapped criterion (measured at 89% of one real bundle's bytes). **Fixed for real in M007/AC011/T010**: a new, additive-optional task field, `mapped_ac_ids: string[]`, absent on every M001-M006 historical task (non-breaking); when present, `buildTaskContextBundle` filters `contractExcerpt.acceptanceCriteria` to exactly those ids; when absent, behavior is byte-for-byte unchanged from before. Disclosed non-bootstrap gap: M007's own contract/tasks.yaml were already confirmed via `milestone-add` before this field existed, so M007's own tasks cannot retroactively use it — real use starts with M008's own contract-drafting, the first milestone drafted after the fix landed. M006 also extended decision 1's disclosure explicitly to dispatched *subagents*, not only the main session: PitWay claims only that the supplied bundle is bounded, never that a subagent's total context is (harness-injected system prompt, tools, skills, memory are outside PitWay's control or visibility) — and it builds no runtime enforcement preventing a subagent from reading a file outside its declared `context_files`; only the *write* boundary (`write_scope`) is mechanically enforced, at commit time. **Resolved in M008/AC004/T003: advisory-only, a permanent-until-revisited non-goal.** M006 deferred the read-enforcement question "to an M007 decision"; no M007 AC addressed it directly; M007/T013 explicitly assigned its disposition to M008. M008's own decision, held to a real technical bar (an isolated worktree alone is not enforcement — it doesn't stop a worker's own shell/tool reads or escape paths): both real candidates that would satisfy that bar — an actual OS-level sandbox around the worker, or a tool-permission allowlist genuinely enforced by the dispatching harness — require either PitWay abandoning its own agent-agnostic architecture (it does not spawn or sandbox workers itself, decision unchanged) or claiming to enforce something it cannot observe or verify happening inside a harness it does not control. `context_files` therefore stays exactly what this paragraph already states — declarative-only, not a technical restriction — and `write_scope` remains the one boundary PitWay mechanically enforces, at both dispatch and completion time. This is stated identically in `README.md` and is not restated differently anywhere else in this document. Revisit only if PitWay ever adopts an execution harness with a genuinely enforceable permission boundary — not on a fixed schedule, and not by PitWay building its own sandboxing/spawning mechanism.

**M019/AC022/T007: the Agent Interface's provider-agnostic obligations extend to usage propagation.** A dedicated investigation found task-level `usage` at 100% `null` across every milestone not because no signal exists, but because no driver protocol document ever instructed the extraction/propagation step for a dispatched worker's already-available, runtime-reported usage figure (visible directly in a dispatch's own tool-result metadata). Closed as a driver-integration MUST-requirement, not a documentation suggestion: when a dispatched worker/subagent's tool result reports runtime usage, the driver MUST extract it and pass it to `task-update <id> completed --usage <json>` (`src/integrations/claude/dispatch.md` step 8; `protocol-driver.md` "Dispatch discipline"). Framed here, not only in the Claude-specific adapter docs, so any future driver adapter (a hypothetical OpenCode adapter, still deferred per §15) inherits the same obligation before it can be considered a compliant PitWay driver integration. This does not extend to inline (non-dispatched) execution, where no task-scoped runtime figure is accessible — `usage` correctly stays `null` there, per decision 8's existing "never estimate" principle (§12), which this requirement restates rather than weakens.

No in-process plugin API, no adapter SDK, no additional adapters in MVP (spec §3/§39).

## 9. Claude Code Adapter

Text assets installed by `init` — no runtime code, delivered M006/T003 (protocol split + 12 lifecycle command docs) and M006/T005 (`commands/auto-run.md` + `interactive-ux.md`), 46 files under `src/integrations/claude/` as of the post-M019 `quick-change` (37 through M018; M019 adds 8: one new command doc, `commands/milestone-merge.md`, plus 7 `commands/ms-*.md` alias docs, 37 → 45; a post-M019 `quick-change`, `qc-7e6fb2a4`, adds the 8th alias doc, `commands/ms-merge.md`, 45 → 46 — see the bullet below), zero `.ts` files, zero Claude/Anthropic SDK imports anywhere in the codebase (structurally tested):

- `.claude/commands/`: `milestone-add.md`, `milestone-cancel.md`, `milestone-confirm.md`, `milestone-status.md`, `milestone-list.md`, `milestone-merge.md`, `milestone-review.md`, `task-status.md`, `task-add.md`, `task-update.md`, `task-verify.md`, `task-amend.md`, `resume.md`, `verify.md`, `verification-repair.md`, `milestone-complete.md`, `usage-add.md`, `write-ms-artifacts.md`, `auto-run.md`, `quick-change.md`, `task-dispatch.md`, `task-integrate.md`, `task-discard.md`, `backlog.md`, plus the 8 `ms-add.md`/`ms-cancel.md`/`ms-complete.md`/`ms-confirm.md`/`ms-list.md`/`ms-merge.md`/`ms-review.md`/`ms-status.md` alias docs (byte-identical bodies to their canonical counterparts — §7) — thirty-two docs for the twenty-five commands (§7), since 8 of them are pure aliases of an already-listed command; `init` gets no asset (developer-run once, self-documents via `--help`). Each is thin: it names the domain use case and points to the driver protocol document, never restating `--help` output.
- The driver protocol is split, not a single document: `protocol-driver.md` (main-session-facing — the one rule, dispatch discipline, decision gates) and `protocol-worker.md` (the fixed text accompanying a `--context` bundle to a dispatched subagent, and nothing else — a dispatched worker's installed instructions never include the full driver document). Supporting documents: `dispatch.md` (the bounded-dispatch sequence and its honesty boundary, see §8), `report-format.md` (capped worker reports), `lsp-guidance.md` (optional LSP use — guidance only, PitWay has no LSP detection/integration code of its own and does not manage a driver's tool availability), `coordination.md` (fresh-snapshot discipline after each dispatched subagent, citing the existing `src/git/safety.ts` primitives by name), and `interactive-ux.md` (the two arrow-key decision prompts and every auto-run invalidation gate, computed and live-checked — see §7's `auto-run` row).
- Driver protocol rules (unchanged in substance from the original design, now actually shipped): all state reads/mutations via `pitway` CLI, never edit `.pitway/` directly; no implementation before the milestone is confirmed; present the full contract to the developer and run `pitway milestone-confirm <id>` only after their explicit approval in the conversation (resolved question #3); execute tasks by spawning subagents whose input is the `--context` bundle and nothing else; report `--usage` only from runtime-reported numbers, otherwise omit; report results as concise structured summaries; never include provider or session metadata in proposed commit messages (PitWay trailers only — the git module also strips such lines mechanically, see §10); on contract conflict stop and follow the change flow (decision 12). Explicit clarification added in M006: PitWay does not programmatically spawn Claude agents anywhere in this codebase — the installed documents instruct whatever agent driver is already running on how to use its own tooling; PitWay supplies instructions and small provider-neutral pure helpers (e.g. `checkWriteScope`), never an agent-spawning mechanism.
- Bootstrap disclosure (`interactive-ux.md`, mirrors M005's own pattern): M006 uses M005's journal/checkpoint model normally for its own real lifecycle; the only gap is that its own interactive confirmation UX did not exist yet at M006's baseline confirm, so M006's own confirm necessarily happened through the pre-existing, non-interactive `milestone-confirm` command.
- M007 additions (T009, T012 — text-only, same 20-file count, no new assets): `dispatch.md` gains a "Choosing inline vs. sub-agent dispatch" rule (inline default for docs/review/small-scope work, `tdd` strategy alone never forces dispatch, sub-agent only for material isolation/concurrency benefit weighed against measured ~32K-token startup overhead, contract-mandated dispatch never overridden, driver decides autonomously); `protocol-driver.md` gains a pre-dispatch conflict-preflight rule (compare composed fixed worker rules against task-specific instructions before dispatching, discovered live when M007/T001's own dispatch had to be stopped and corrected mid-flight) and a tiered Decision Authority Policy (autonomous / continue-and-batch-report / mandatory developer gate, governing how much confirmation a driver decision needs during an auto-run — auto-run authorization never overrides the mandatory-gate tier); `protocol-worker.md` gains an explicit git-free RED-check toggle pattern (rename via Edit/Write, never `git stash`) and reinforces the always-bounded-timeout / no-unattended-background-command rules.
- M009 additions (T001-T005 — three new command docs, one protocol-driver.md addition, 20 → 23 files): `commands/milestone-cancel.md`, `commands/verification-repair.md`, `commands/quick-change.md` added, each in the same thin-pointer style as every existing command doc; `protocol-driver.md` gains a "Choosing a correction mechanism" section naming when to reach for `quick-change` (a small, bounded fix against an already-completed milestone, fits one atomic commit, no architecture/schema/API/dependency/security/migration/multi-subsystem impact) versus a one-task corrective milestone (anything bigger) versus a full milestone (new capability work) — the first committed driver-facing guidance for this choice; before M009 it existed only as prose in this document's own Revised Roadmap narrative, never as an asset a driver session actually installs and reads.
- M010 additions (T003 — one new command doc, two targeted protocol-doc insertions, 23 → 24 files): `commands/task-verify.md` added, documenting the three-way distinction between a verification record (the full `task_verify_evidence` journal entry), its evidence id, and the persisted result evidence (the capped string in `task.result.evidence`), and stating plainly that once any evidence record exists for a task, plain `--result`/`--message` completion can no longer bypass it. `protocol-driver.md` and `dispatch.md` each gain a small, targeted insertion describing the driver running `task-verify` (while a task is still `in_progress`) as the formal, journaled replacement for an ad hoc independent rerun-and-eyeball of the verification command — explicitly preserving, not replacing, the driver's own diff/`write_scope` review step, which still runs first, every time.
- M011 additions (T001, T006 — six vendored skills + one attribution file, two targeted protocol-doc insertions, 24 → 31 files, no new command docs): `src/integrations/claude/skills/{debugging,bug-fix,testing,code-quality-review,architecture-review,security-audit}/SKILL.md` vendored verbatim, byte-for-byte, from `github.com/thixpin/claude-config` at pinned commit `d498eae219441839f75f737643857f6c0f84df12` (MIT, developer-owned; `infra-design`/`terraform-review` explicitly evaluated and rejected — no PitWay infrastructure/IaC domain); `skills/NOTICE.md` records the source repository, pinned commit, license, and the copyright notice copied verbatim from the pinned commit's own `LICENSE` file. Installed automatically by the existing glob-based installer with zero installer-discovery code changes. A new, additive-optional `tasks.yaml` field, `required_skills: string[]` (0-2 kebab-case names), gates `task-status <id> --context` via a pure Core comparison (`assertRequiredSkillsAvailable`) composed with a State-layer read of what's actually installed (`listInstalledSkillNames`) — a **pre-dispatch context gate**, not "dispatch-time enforcement": it proves PitWay's own managed skill installation is present, never that the dispatching harness actually loads it (same disclosed limitation as §8's advisory-only worker read-boundary disposition). `protocol-driver.md` gains a "Skills" section (when/how to declare `required_skills`, informal/voluntary skill loading, `code-quality-review` never replacing the mandatory independent review step); `dispatch.md`'s existing pull-context-bundle step now documents that this is where the pre-dispatch gate fires and that a refusal blocks dispatch.
- M012 additions (T008 — one targeted protocol-doc insertion, 31 files unchanged, no new command docs since `branch_strategy` surfaces entirely through existing commands): `protocol-driver.md` gains a "Verification discipline" section (task execution runs only its own declared command; full-suite/typecheck runs are milestone-level gates only; recorded manual/review verification results are authoritative, never re-derived by re-invoking bare `verify`) — codifying, as a real installed asset, a developer correction given live during M011's own execution.
- M013 additions (T007 — one targeted protocol-doc insertion, 31 files unchanged, no new command docs since `verify --status`/`milestone-status --report` are read-only flags on existing commands): `protocol-driver.md` gains a "Progress reporting" section (the ADD-style concise routine-update convention; the racing-footer-after-confirmation-only rule, with no footer or footer-explanation text before that; one-line descriptions of the two new read-only flags) — explicitly disclosing, the same way the Skills section discloses `required_skills`, that PitWay can prove the underlying data/rendering is correct but cannot verify a driver session actually follows the habit.
- M014 additions (T010 — three new command docs plus four targeted protocol-doc insertions, 31 → 34 files): `commands/task-dispatch.md`, `commands/task-integrate.md`, `commands/task-discard.md`; `protocol-driver.md` gains a "Parallel dispatch" section (config gating, when to parallelize, the dispatch → driver-passes-bundle → worker-commits → integrate → authoritative-verify → review → completed sequence, ascending-task-id integration as a stated driver convention, recovery mapped to `resume`'s residue classes, the explicit disclosure that PitWay gates eligibility/scope/state but never spawns/schedules/monitors workers and cannot verify actual concurrency, and QA read-only/dedicated-worktree guidance); `coordination.md` gains the parallel-mode counterpart (the stale-snapshot hazard moves to the integrate boundary; a worktree's committed `.pitway/` copy is stale read-only transport with an empty per-worktree journal); `protocol-worker.md` gains the worktree contract (worktree-only, local commits on the scaffolding branch as the one sanctioned `git` relaxation, report the branch HEAD SHA, marker untouched); `dispatch.md` gains the worktree-mode dispatch flavor. Asset discovery remains fully dynamic — no manifest exists; shipped-and-installed coverage is asserted by tests instead.
- M015 additions (T010 — one new command doc plus one targeted protocol-doc insertion, 34 → 35 files): `commands/milestone-review.md` (the `/milestone-review` driver surface: select roles with the developer, dispatch one reviewer subagent per role with only its bounded `brief --json` envelope, record findings verbatim, present the report, the developer decides — three example role combinations quoted as examples, never enforced presets); `protocol-driver.md` gains a "Milestone review" section with the same flow plus the advisory-honesty register: no review command can mutate the milestone; an unsanctioned contract/task-definition edit is DETECTED by the hash gate at the next `brief`/`record`, never prevented; PitWay never runs reviews and cannot verify reviewer independence or concurrency; a recorded finding is reviewer opinion, never implementation evidence; the documented practice is decide-before-`milestone-complete`. Asset discovery stayed fully dynamic — zero installer-discovery code changes, confirming M007/T008's and M011's own prediction a third time.
- M016 additions: none — validation-only, no new command, no asset change.
- M017 additions (T002 — one new command doc, 35 → 36 files): `commands/task-add.md` (the sanctioned mid-flight task-insertion surface — §7); `protocol-driver.md`'s "Choosing a correction mechanism" section gains `task-add` as an option alongside `quick-change`/one-task-corrective-milestone/full-milestone, cross-referencing `milestone-cancel.md`'s new confirmed-milestone boundary paragraph (cancel is draft-only by design, never a tool for abandoning an active milestone; abandon remaining work by cancelling not-yet-started tasks and completing, never `git reset`); `protocol-driver.md` also gains a "Drafting write_scope" section naming the concrete assertion sites (registered-command list, baseline/`completionPaths`, shipped-asset tests, any exact-string test) to grep before finalizing a task that changes a count, enumerated list, or persisted/rendered string shape — codifying the Backlog's `write_scope`/stale-test-assertion pattern (four prior recurrences) as an installed, driver-facing checklist rather than a lesson re-learned per milestone.
- M018 additions (T005 — one new command doc, 36 → 37 files): `commands/backlog.md` (the deferred-work-capture surface — §7); `protocol-driver.md`'s "Choosing a correction mechanism" section gains `backlog` as the third option, distinguished from `task-add` (in-scope insertion) by scope, not by mechanism — both require an active milestone, but `backlog` never touches `tasks.yaml`/`contract.md` at all.
- M019 additions (T002-T003, T007 — two new command docs, 7 alias docs, several targeted protocol-doc insertions, 37 → 45 files): `commands/milestone-merge.md` (the merge-ready-to-merged surface — §7); 7 byte-identical `commands/ms-*.md` alias docs for the 7 named milestone-* commands (T003 — no generation tooling, a byte-parity test is the sync discipline instead); every command doc's `description:` gains a `PitWay: ` prefix (T003 — the 6 vendored skills excluded, their frontmatter is part of the byte-for-byte vendoring guarantee); `dispatch.md` and `protocol-driver.md` gain the runtime usage-propagation MUST-requirement (T007 — see §8's addition above), cross-referenced from `commands/task-update.md`.
- Post-M019 `quick-change` (`qc-7e6fb2a4`, developer-requested follow-up, 45 → 46 files): `commands/ms-merge.md` added — the 8th `ms-*` alias doc, for `milestone-merge`, closing the gap M019/AC013 disclosed as deliberately excluded since it postdated the backlog item's fixed command list; `src/cli/commands/milestone-merge.ts` gains the matching `.alias('ms-merge')`.

Usage capture: when the Claude Code runtime reports subagent token usage, the driver passes it to `task-update --usage`; when it doesn't, usage stays `null` → `N/A`. Never estimated (decision 8). **Formalized as a MUST-level driver-integration requirement, not only descriptive prose, in M019/AC022/T007** — see §8's addition for why: the mechanism was always correct, but no driver document ever instructed the extraction/propagation step until this milestone.

## 10. Git Strategy

- **Clean-tree-at-task-start invariant:** `task-update --status in_progress` runs the safety check; a dirty tree stops with "ask the developer" (spec §26) — PitWay never stashes/resets/absorbs. *Why at start:* PitWay cannot classify dirty files as related/unrelated at commit time; requiring a clean start makes every change at completion attributable to the task, deterministically.
- **One atomic commit per completed task** containing the code changes *and* the same-task `.pitway/` state update. Message: driver-proposed (repo convention) via `--message-file`; PitWay appends trailers `PitWay-Milestone: M001` / `PitWay-Task: T002`. **Commits PitWay creates in managed projects are provider-agnostic**: the git module sanitizes every driver-proposed message, stripping provider/session metadata lines (`Claude-Session:`, `Co-Authored-By: Claude`, `Codex-Session:`, `Gemini-Session:`, and the like) before committing — mechanical enforcement, not protocol discipline — so PitWay trailers are the only workflow metadata in a managed repo's history, and no PitWay behavior ever depends on an AI conversation or session identifier. SHAs are resolved from trailers when needed and never duplicated into state (decision 4). Robustness assessment (extended 2026-08-19 with M004 findings): trailers survive ordinary rebases (content-stable) and work with `git log --grep`, but the limitations are real — shallow clones may not see old history; squash-merges and history rewrites can drop, merge, or duplicate trailer lines, breaking the one-commit-per-identity assumption; and unbounded `--grep` scans cost grows linearly with repository history. Acceptable for the sequential MVP since nothing functional depends on SHA lookup (traceability plus resume-identity checks only), and every identity check re-verifies the committed content, never trusting the trailer alone. **Delivered in M012:** `resolveCommitSha` (`src/git/trailers.ts`) gains an additive-optional `since` range parameter, bounding the search to `since..HEAD` when a milestone's persisted `base_revision` is available (`branch_strategy: milestone`) — every call site with a milestone in scope passes it when present and omits it otherwise, preserving the original unbounded scan exactly for `main`-strategy milestones and every milestone confirmed before M012. Deliberately **not** a silent-fallback design: an unreachable/invalid `since` throws a `GitError` naming it explicitly rather than widening back to an unbounded scan — a silent fallback would defeat the branch-isolation guarantee `base_revision` exists to provide, and could, once completed milestone branches are eventually merged, walk into an unrelated milestone's own merged history. A genuinely empty (but valid) range still returns `undefined`, exactly as an unbounded "not found" already did. No simpler alternative found; adopted as proposed.
- **Baseline commit** at `milestone-confirm` (`workflow: add milestone M001` + milestone trailer) — this is also the moment the milestone's `.pitway/` artifacts get committed, closing the state/commit circularity at a defined boundary. Never empty (spec §27).
- `.pitway/` is version-controlled (decision 3); `init` does *not* gitignore it; runtime-disposable data is simply never written there.
- Never RED states, retries, or intermediate edits committed; no stashes and no automatic merges, ever. **Sequential execution remains the default** — byte-identical whenever `execution` is absent from `config.yaml` — and **`execution.strategy: parallel_worktrees` is the delivered opt-in (M014)**: eligibility-gated per-task temporary worktrees with diff-apply integration (see the M014 roadmap entry). The per-task-branch principle, reworded to its delivered precise form: **tasks never own PERSISTENT branches** — milestones may own a branch (M012), while the `pitway/task/<mId>-<tId>` scaffolding branch a dispatch creates exists only between `task-dispatch` and `task-integrate`/`task-discard`, never enters mainline history (its content lands as an applied diff inside the task's own atomic commit — never a merge), and is always deleted with its worktree. This rewording resolves an internal contradiction this document had carried since the roadmap's own M014 entry sanctioned "one temporary worktree + task branch per dispatched task" against the former absolute "no per-task branch of any kind" wording here — resolved in the roadmap's favor, with persistence as the real invariant. The one-atomic-commit-per-task invariant is preserved byte-for-byte under both strategies, with one disclosed, precedented nuance: a task's completion commit may carry a concurrently-dispatched sibling's `in_progress`/attempts state in the same `tasks.yaml` write, exactly as dependent-task promotion riders already ride along today (`findCompletionCommit`'s parsed comparison ignores sibling changes by design).
- **Branch strategy is a repository-level policy (`git.branch_strategy: main | milestone`), delivered in M012.** `main` remains the default whenever `git` is absent from `config.yaml` — byte-identical to every pre-M012 behavior: PitWay never creates or switches branches, task and baseline commits land on whatever branch is currently checked out. Under `milestone`: `milestone-confirm`, after its existing dirty-tree safety check passes, creates one deterministically-named branch (`pitway/<id>-<slug>`) from the currently checked-out branch and checks it out — the only branch mutation confirm ever performs on a fresh confirm; it refuses outright, never reuses or resets, if that branch name already exists (a genuine foreign collision and PitWay's own orphaned half-created branch from a crashed attempt are deliberately not distinguished — both refuse identically, requiring manual developer inspection). The originating branch name and revision are persisted as `base_branch`/`base_revision` in contract frontmatter (additive-optional, nullable — null for `main` strategy and every milestone confirmed before M012), never hardcoded to `main`. A resumed confirm (the existing AC012 shape) performs *no* branch mutation at all: it refuses with a diagnostic naming both branches unless the currently checked-out branch is exactly the milestone's own expected branch, including when that branch was deleted between attempts — the developer switches manually. A shared guard (`assertOnMilestoneBranch`) is wired into every commit-producing operation for an active `milestone`-strategy milestone (task completion, milestone-complete) and refuses on the same mismatch before staging or committing anything; it never auto-checks-out to fix a mismatch, matching confirm's own resume behavior. `pitway resume` surfaces a tracked branch and any mismatch for orientation only — read-only, it never runs a checkout itself. `milestone-complete` reaches a merge-ready/PR-ready state (the milestone branch contains exactly its baseline/task/completion commits from `base_revision`, the base branch is left completely untouched, no merge commit is ever created) without ever performing an automatic merge, force-push, or branch deletion. **Disclosed, real limitation found during M012's own end-to-end test (T007):** `.pitway/state.yaml` and a milestone's own directory are committed content that exists only on that milestone's branch until it is eventually merged — a developer who checks out all the way back to the true pre-milestone base branch (not merely a sibling branch) will find every `pitway` command, `resume` included, fails with a plain file-not-found error rather than a graceful mismatch report; this is a pre-existing, out-of-scope limitation (any `pitway` command already fails this way outside an initialized repo), not something M012's own mismatch-detection is expected to paper over. See §15 for the original design sketch this delivery followed.

## 11. Verification Strategy

Three CT types (decision 6): `command`, `manual`, `review`, defined in contract frontmatter, each mapped to an AC.

**Approval boundary:** `milestone-confirm` canonicalizes the `verification` list and stores `verification_approved_hash` in the frontmatter. `pitway verify` recomputes the hash first and refuses to run on mismatch, directing to the amendment flow. Through M004 this meant hand-editing `contract.md` directly, then running `milestone-confirm --amend` to recompute/approve/commit; as of M005/T004, direct edits to `contract.md` are prohibited — `--amend --file <path>` takes a validated draft instead, journal-records and materializes immediately (no commit of its own), and a Change Log entry plus developer approval remain mandatory either way. *Why a hash rather than a frozen copy:* one file stays authoritative; the hash records "what was approved" as a separate fact without duplicating the definitions. This mechanically enforces "agent-authored commands never silently become trusted" — an edited or added command cannot execute until a human re-confirms.

Execution: `command` CTs run via shell in the repo root, exit code ⇒ pass/fail, evidence = trimmed output summary. `manual`/`review` CTs are recorded by the developer (`verify --check CT002 --pass --evidence "…"`), stamped `recorded_by: developer`. Milestone `review → completed` requires: all required tasks completed, all CTs pass, Senior QA review recorded at the completion boundary (spec §14).

**Verification-execution hardening (M006/T001-T002, closing a confirmed M005 defect — see M005 report.md §7/§13):** `command` checks carry an additive-optional `timeout_ms` (integer, 1–3,600,000ms, default 120000ms when omitted; command-type only, rejected on `manual`/`review` by schema discrimination; covered by the approval hash since it hashes the raw `verification:` block text). Execution runs through a dedicated process-execution helper with platform-aware descendant-process cleanup on timeout (POSIX: process-group `SIGKILL`; Windows: `taskkill /t /f`, implemented and decision-tested via mocks, not live-verified — no Windows CI exists for this repository). Each check's result — including a `duration_ms`/`termination_reason` (`exited | timeout | signal | spawn_error`, both additive-optional) — is persisted immediately after that check completes, not only once after the entire loop, so a later check's hang or timeout never discards earlier results. A repo+milestone-scoped recursion guard refuses a verification command that would re-enter the same live repository's verification of the same milestone, while explicitly permitting nested verification of an unrelated repository or milestone (needed for this project's own test suite, which spins up synthetic temp repos). `verify <id> --check CTnnn`, invoked without `--pass`/`--fail`/`--evidence` on a command-type check, re-executes exactly that one check in isolation through the same hash-gated, timeout-protected path — Core never retries automatically; retry-or-diagnose policy is the Claude driver's, applied via the protocol assets and interactive decision UX, never Core's.

## 12. Token Accounting Strategy

Decision 8, operationalized: usage recorded only when runtime-reported; `null` ⇒ `N/A`; task usage accumulates across attempts (`attempts` counter + summed totals — retries are real cost); milestone-level planning/QA usage recorded separately via `usage-add`. Aggregation: milestone total = Σ measured task usage + measured milestone-level usage; tasks with `null` are excluded and *surfaced*, e.g. `Tokens: 84.2k (2 tasks N/A)` — measured and unavailable are never blended (spec §33). No estimates anywhere, including progress: milestone progress = `completed required tasks / total required tasks` (decision 5); task rows show status glyphs only, no percentages.

**M019/AC022/T007**: the persistence/aggregation mechanics above were already fully correct; the producer side was not — see §8's addition for the closed driver-integration gap (dispatched-worker usage extraction/propagation is now a MUST) and risk 2 below's revision.

## 13. Testing Strategy

Vitest, three tiers (spec §48):

- **Unit** (pure Core, no I/O): task/milestone state machines (every legal and illegal transition), dependency resolution incl. cycles, progress calculation, usage aggregation (null-mixing, double-count guards), contract frontmatter parse/validate, verification hash canonicalization, git safety decision logic (git calls mocked).
- **Integration** (real temp git repos, CLI invoked end-to-end): `init` (incl. non-git refusal), `milestone-add` from fixture artifacts, `milestone-confirm` (baseline commit + trailer + hash), task lifecycle with atomic commit + trailers, `resume` from cold state, `verify` with stub commands and the hash-mismatch refusal.
- **Workflow**: one simulated milestone requirement→completion, plus the six spec cases — task failure, blocked dependency, cross-session resume, contract change (amend flow), dirty working tree, missing token usage.

PitWay's own tasks follow task-level TDD where appropriate (decision 13); non-code tasks (docs, templates) use `manual`/`review` verification per spec §24.

## 14. MVP Scope

Everything in §§1–13 above and nothing more, proving spec §55's eight points: repo-local state (schema v1) · contract lifecycle with human gate · task graph with sequential execution · TDD workflow support via context bundles · safe git checkpoints (baseline + atomic trailer commits) · status/resume from cold state · Claude integration without core coupling · deterministic context minimization. Plus npm packaging (`pitway`, verified unclaimed on npm as of 2026-08-18 — claim early) and the spec §49 README.

## 15. Deferred Features

Codex/Gemini/OpenCode adapters · plugin/adapter SDK · weighted or per-task progress · contract versioning beyond the changelog · schema migration framework · requirement/BRS tooling beyond a plain markdown template · milestone archiving/pruning · `pitway doctor` · non-git support · any web/UI anything (non-goal).

### Delivered in M012: milestone-level Git branch strategy

**Architectural principle (durable, reworded by M014): milestones may own branches; tasks own commits — and never own PERSISTENT branches.** Durable branch isolation is at the milestone level only (`pitway/M001-auth` holds all of T001..Tn's commits); M014's `pitway/task/*` scaffolding branches are transient execution scaffolding, never part of history (see §10). This preserves the MVP rule that sequential execution and atomic task commits are the core workflow; branch isolation is an optional Git policy layered on top, never a prerequisite for it. M012 delivered exactly this, with no material deviation from the design sketch this subsection originally carried — see **§10** for the real, delivered behavior (config shape, the fresh-confirm/resumed-confirm split, the commit-branch guard, `resume` surfacing, range-bounded trailer lookup, merge-ready completion, and the one disclosed limitation found during delivery). This subsection is kept as the historical planning note it was, not restated here.

**M002 was and remains unaffected** — its confirmed contract already excluded branches, worktrees, stashes, and merges, and that boundary was preserved rather than modified by M012's own delivery.

### Delivered in M014: parallel task worktrees

The former "parallel execution, worktrees" deferred item is real — see the M014 roadmap entry and §10's rewritten branch/sequential lines for the delivered shape. Sequential execution remains the default and byte-identical; parallelism is a committed, opt-in repository policy (`execution.strategy: parallel_worktrees`), never a prerequisite for anything.

## 16. Risks / Trade-offs

1. **The human gate is procedural, not physical.** A misbehaving driver could run `milestone-confirm` without real approval. Mitigation: the protocol requires explicit developer approval in the conversation before Claude runs `milestone-confirm` (resolved question #3), and the command prints the contract summary before acting; residual risk accepted for MVP.
2. **Usage data is best-effort.** If the runtime doesn't expose subagent usage, milestones show mostly `N/A`. Accepted by design; never estimated. **Revised (M019/AC022/T007)**: for a dispatched worker, the runtime *does* expose usage — the gap found was that no driver protocol document ever instructed capturing and forwarding it, now closed as a MUST (§8, §12). The residual, unchanged best-effort case is inline (non-dispatched) execution, where no task-scoped runtime figure is genuinely accessible.
3. **Session context bleed.** Claude-driven means the main session sees more than the task bundle. Accepted (decision 1); the bundle still bounds what *task subagents* see.
4. **Frontmatter drift by hand-editing.** A human editing `contract.md` outside the flow can desync approved state. Mitigated by the verification hash (execution refuses) and load-time schema validation; not fully preventable.
5. **Clean-tree-at-start friction.** Developers with WIP must park it before a task starts. Deliberate: determinism over convenience (spec §51).
6. **Trailer lookup needs history.** Shallow clones can't resolve old task SHAs. Traceability-only impact; documented.
7. **Bootstrap overhead.** Phase 1 (manual protocol) is slower than freestyle coding. It doubles as validation of the schema and workflow before code enforces them — cost accepted.
8. **Task definitions had no supported amendment path** (discovered 2026-08-19, materialized twice within M004 itself): commit `179491c` hand-edited T004's task definition because no command existed for it (disclosed by name in M004's amended AC014 — history preserved, not rewritten), and T006 had to be cancelled rather than have its `relevant_files` corrected when its true blast radius surfaced. Until M005's validated task-amendment command (with the `context_files`/`write_scope` split) lands, a task definition changes only by cancel-and-replace through existing commands, never by hand-edit.

## 17. Open Questions Requiring Developer Decisions

All resolved by the developer on 2026-08-18:

1. **Stack**: approved — TypeScript strict / Node ≥ 20 / ESM / vitest / commander + yaml + zod as the only runtime deps.
2. **License**: MIT.
3. **Confirmation mechanics**: Claude may execute `milestone-confirm` itself, but only after explicit developer approval in the conversation; the developer is not required to type the command.
4. **`init` installs Claude integration by default**, with `--no-claude` opt-out.
5. **npm name**: target unscoped `pitway`; re-check registry availability immediately before publishing.

---

# Bootstrap → Self-Hosted Transition (decisions 13–14)

**Phase 1 — complete.** M001–M003 were executed under the manual PitWay protocol: milestone/contract/task artifacts hand-authored in `.pitway/` following schema v1 exactly, manual commits carrying PitWay trailers, TDD and atomic-commit rules from the first commit.

**Transition criterion — satisfied 2026-08-18.** After M003, `pitway resume` correctly reported M001–M003 from the hand-authored state; M004 was then created, confirmed, executed, verified, and completed through PitWay commands (baseline `c3bff98` → completion `5a99528`, all trailers written by PitWay itself).

Completed milestone record (historical evidence — preserved, not rewritten to match later architecture):

| Milestone | Delivered | Mode |
|---|---|---|
| M001 | Project scaffold + state layer: schema v1 types, load/save/validate `.pitway/`, task & milestone state machines | manual |
| M002 | Git module (safety check, trailer commits, baseline) + read commands: `milestone-status`, `milestone-list`, `task-status` (incl. `--context`), `resume` | manual |
| M003 | Mutation commands: `init`, `milestone-add`, `milestone-confirm`, `task-update` (atomic completion commits) | manual |
| M004 | `verify` (3 CT types + approval hash), `milestone-complete`, `usage-add` + honest aggregation, dependent auto-promotion + resume continuation priority, requirement-store refactor, CLI reachability (all 11 commands on the single entry point). Slugged directories (AC012/AC013) removed by disclosed amendment and deferred; T006 cancelled via `task-update`. | self-hosted* |
| M005 | Resumable git-invisible runtime journal (checkpoint-eligible entries + markers, self-healing recovery); PitWay-expected vs. unexpected dirty-path classification; `context_files`/`write_scope` schema split (5-case combination rule); checkpoint-model revision (`usage-add`/`milestone-confirm --amend` journal-record-then-materialize, no commit of their own); `task-amend` (validated task-definition amendments, cumulative chaining); `write-ms-artifacts` (non-authoritative draft output); slugged milestone directories effective M006 onward (M001–M005 grandfathered bare); migration verification; roadmap reconciliation (T009). Built and completed entirely under the pre-existing M004 checkpoint mechanics — the model it delivers becomes authoritative starting with M006's own lifecycle, not retroactively true of M005's own history. | self-hosted |
| M006 | Verification-execution hardening (per-check `timeout_ms`, platform-aware process-tree cleanup, recursion guard, incremental result persistence, isolated single-check rerun); installable Claude Code integration assets (20 files, driver/worker protocol split, dispatch honesty, capped worker reports, LSP guidance, shared-worktree coordination guidance); `auto-run enable\|disable\|status` (agent-agnostic authorization, new `auto_run` journal kind); interactive arrow-key decision UX design (`interactive-ux.md`); measured context-efficiency evidence against the M004/M005 baseline, including the (then-unfixed) context-bundle duplication defect. Three standalone hotfixes landed during execution. First milestone whose entire lifecycle used M005's journal/checkpoint model as authoritative from baseline confirm onward. | self-hosted |
| M007 | Real-history dogfood evidence (fresh-session resume regression test, a Git traceability audit against synthetic history proving generic classification, confirming 7 standalone hotfix commits across M005+M006 plus one between-milestone maintenance commit correctly classified separately, and a new finding — an early-history rewrite with zero common ancestor to `main`); the context-bundle duplication defect fixed for real (`mapped_ac_ids`); right-sized dispatch mode adopted and formalized (`protocol-driver.md`/`dispatch.md`); a tiered Decision Authority Policy and a pre-dispatch conflict-preflight rule, both discovered live during the milestone's own execution; a first-class tooling-defect recovery procedure (4 worked examples) and a completed-task-revision path (real tested mechanism, zero new Core code); three architectural findings evaluated (contract-fixed/task-adaptive, write-scope boundary shape, ripple-fix policy) and the Adaptive Workflow Intensity decision itself. Zero standalone hotfix commits during its own execution — a first. | self-hosted |
| M008 | A real `tsc`-based build (`tsconfig.build.json`, separate from the untouched `noEmit` `tsconfig.json`) closing the M004/T007 finding for real, plus a cross-platform asset-copy script shipping the Claude Markdown assets alongside compiled code; complete, explicit npm packaging metadata validated by a real `npm pack` → fresh-install → real-binary-invoke check, which caught and fixed a real symlink-invocation entry-point defect (`realpathSync`); the worker read-boundary-enforcement question resolved (advisory-only, permanent-until-revisited); a real, honest `README.md`. Release readiness only — `npm publish` never ran. One post-completion exception: a shared-`dist/`-test-race verification repair (`6f8b5e6`/`ed119ed`), a standalone trailer-less commit since no supported repair path existed yet — the exact gap M009/AC002 closes. | self-hosted |
| M009 | `milestone-add --replace` (draft-only, same-id, no-git-operation in-place correction, replacing the ad hoc git-restore-and-relocate workaround M008's own draft correction needed) alongside `milestone-cancel` (the separate preserve-and-retire path, id never reused); `verification-repair` (a real, two-phase, approve-before-edit, VR-id-identified, dual-trailer mechanism for the tasks-complete/pre-`milestone-complete` window M008's `6f8b5e6`/`ed119ed` standalone commits had occupied); the full `quick-change` workflow (`create\|approve\|run\|commit\|cancel\|promote\|status`) against an already-completed milestone, `PitWay-Change`-trailer commits, `pitway resume` as the authoritative recovery view. Zero standalone hotfix commits during its own execution — every ripple effect folded into the discovering task's own commit via an approved `task-amend`. One disclosed post-completion exception: a bootstrap-circularity fix to `verification-repairs.yaml`'s own store loader (`13865d4`) — no repair path existed yet to receive it, so it was closed as a standalone developer-approved fix rather than through `quick-change`. | self-hosted |
| M010 | A new `task-verify <id> [--typecheck <command>]` command executing exactly an `in_progress` task's own approved `command`/`tdd` verification command, capturing exit code, best-effort pass/fail counts, duration, and termination reason; the fingerprint covers exactly the task's declared `write_scope`/`relevant_files`, never `.pitway/` workflow state, settled after two architecture-review CRITICAL findings converged on a dirty-path allowance mirroring `completeTask`'s own already-shipped `expectedPaths` construction. Evidence persists as a git-invisible, resumable `task_verify_evidence` journal record; `task-update`'s completion path resolves it implicitly or via `--evidence <id>`, validating identity/run success/attempt/command/`write_scope`/fingerprint, refusing outright on any mismatch; a task with no record falls through byte-for-byte unchanged to the pre-existing `--result`/`--message` path. A real end-to-end integration test proves `review`'s own `tasks.yaml` rewrite never invalidates recorded evidence. Zero standalone hotfix commits during its own execution. | self-hosted |
| M011 | Six Claude Code skills vendored verbatim, byte-for-byte, from `github.com/thixpin/claude-config` (developer's own MIT-licensed repository) at a pinned commit, plus a `NOTICE.md` attribution file; `infra-design`/`terraform-review` explicitly evaluated and rejected. A corrected, per-file content-comparing `.claude/` classification (`classifyClaudeAssets`) replaces the former coarse present/missing/invalid probe. Two new root files, `AGENTS.md`/`CLAUDE.md`, installed non-destructively and wired into `milestone-confirm`'s baseline staging. A shared classification mechanism (`listSafeManagedDirtyPaths`) closed two real, pre-existing gaps: `quick-change create`/`commit` immediately after a fresh `init`, and content-aware `.claude/` staging at confirm time. A new, additive-optional `required_skills: string[]` task field gates `task-status <id> --context` through a pre-dispatch context gate. Real comparative dogfood evidence recorded honestly as a negative/null single-trial finding. Zero standalone hotfix commits during its own execution. | self-hosted |
| M012 | `git.branch_strategy: main \| milestone` config policy — the first real, non-`schema_version` `config.yaml` field. Under `milestone` strategy, `milestone-confirm` creates a deterministically-named branch (`pitway/<id>-<slug>`) after its safety check passes, refusing outright on any existing-branch-name collision (foreign or PitWay's own orphaned attempt alike); a resumed confirm performs zero branch mutation, refusing on any position mismatch. `base_branch`/`base_revision` persist in contract frontmatter (additive-optional, nullable). A shared guard (`assertOnMilestoneBranch`) refuses a checked-out-branch mismatch before every commit-producing operation, never auto-checking-out. `pitway resume` surfaces a tracked branch and any mismatch, read-only. `resolveCommitSha` gains a `since` range-scoping parameter — an unreachable `base_revision` throws rather than silently widening to an unbounded scan, preserving the isolation guarantee. `milestone-complete` reaches a merge-ready/PR-ready state via real git-plumbing assertions, never an automatic merge. `main` strategy (explicit or absent) is provably unaffected. A `protocol-driver.md` "Verification discipline" section. A disclosed limitation: `.pitway/state.yaml` and a milestone's own directory exist only on that milestone's own branch until merged — checking out past it to the true pre-milestone base fails every `pitway` command, not just this milestone's own mismatch detection. Zero standalone hotfix commits during its own execution; one task-level `write_scope` correction (T005, developer-approved `task-amend`). | self-hosted |
| M013 | PitWay Driver UX: `verify --status` (read-only latest-result view, extracted shared verification-status helper reused by `milestone-complete`'s gate); additive-optional `taskSchema.name` short label surfaced in `resume`/`task-status`; pure `computeWorkloadPercentage` (the five-band cumulative formula) and `computeRacingFooter` (the permanent one-line footer, null before confirmation, wired into `resume`/`milestone-status`); pure `computeCriticalPath`; `milestone-status --report` (structured Progress Report with evidence-honest task labeling via `hasVerifiedEvidence` and a token breakdown deliberately omitting any non-measured driver-overhead figure); a `protocol-driver.md` "Progress reporting" section. One developer-approved mid-execution scope widening (T008, via `task-amend`, after the milestone-level full-suite gate caught two pre-existing tests whose resume-task-list assertions predated the new `name` field). | self-hosted |
| M014 | Opt-in `execution.strategy: sequential \| parallel_worktrees` config (absent ⇒ sequential, byte-identical); a pure eligibility function (`checkParallelEligibility` — dependency-independence + pairwise-disjoint `write_scope` against every `in_progress` task, legacy `relevant_files` tasks never eligible); a Git-layer worktree module (one temporary worktree per dispatched task, refuse-on-collision); `task-dispatch`/`task-integrate`/`task-discard` (the parallel-worktree lifecycle: dispatch to a worktree, diff-apply integration — never a merge — and the abandonment exit); a fail-closed CLI state guard (every command refuses inside a task worktree except a read-only allowlist); `resume` residue classification (four read-only classes covering every crash window); an end-to-end lifecycle test proving mainline history after parallel execution is structurally indistinguishable from sequential. Dogfood irony, disclosed: M014's own tasks shared write scopes and were therefore mutually parallel-ineligible under its own rules — the first real parallel dispatch was left to a later milestone (M016's validation pass). Two standalone developer-directed fixes landed between tasks (M013-surface polish, outside M014's scope, each explicitly requested on a clean tree). | self-hosted |
| M015 | A first-class, role-based milestone review workflow (`reviews.yaml`, a nine-role Core registry, `milestone-review start\|brief\|record\|report\|decide`) layered entirely on top of existing mechanics — reviewers produce findings only, never mutate or confirm the milestone. A definition-hash staleness gate over a canonical contract-CONTENT + task-DEFINITION projection, execution/lifecycle fields excluded on both sides, so task transitions and `milestone-confirm`'s own status promotion never stale a session while a real revision does. Every review mutation journal-backed exactly like a usage recording or amendment (`review_recording`, a fourth entry-kind operation type), so the existing task-flow dirty-tree checks expect it with zero call-site changes; two per-file allowlists gain a `reviews.yaml` row. A plain `node:readline` numbered multi-select for interactive role selection — no new runtime dependency, `interactive-ux.md`'s arrow-key design stays design-only. Two independent pre-confirmation reviews caught real defects folded in before confirmation. One genuine gate-caught defect, corrected via the M012/M013/M014 widening precedent: `content_hash`'s contract-side component was initially raw file bytes, corrected to a content-only projection once a real-lifecycle test caught the contradiction with the milestone's own design decision. Bootstrap disclosure: this milestone's own pre-confirmation review necessarily used the informal M014-style two-reviewer flow, not the `milestone-review` command it delivers — the first real run belonged to M016. | self-hosted |
| M016 | Extended dogfood validation: the first real parallel-worktree dispatch (T001 + T002 dispatched concurrently, integrated via `task-integrate` in ascending id, mainline history proven indistinguishable from sequential) and the first real `git.branch_strategy: milestone` run (the milestone executed on its own `pitway/M016-…` branch, merged into `main` afterward by the developer) in this repository's own history; the three-milestone-recurring nested-full-suite flake root-caused by real reproduction (Vitest's default 5000ms per-test timeout under genuine concurrent-subprocess load — never a PitWay defect) and hardened with a disclosed timeout increase; the M011 `QuickChangeError` identity split and `requireQuickChange` duplication (four files, not three) fixed with regression tests; context-efficiency evidence confirming the M007/AC011 bundle filter on live dispatch data; the first real `pitway milestone-review` run (QA role) at its own drafting, whose findings were folded in before confirmation. Two genuine mechanism gaps found live and recorded in the Backlog rather than absorbed: `milestone-confirm` never reconciling pending journal entries, and no sanctioned path to add a task to or cancel a confirmed milestone — the latter forcing a disclosed, out-of-architecture `git reset` + manual journal trim to recover a clean draft before the final confirm. Zero standalone hotfix commits during its own execution. | self-hosted |
| M017 | Backlog Closure and Lifecycle Hardening: `milestone-confirm`'s baseline commits now call `reconcilePending`, closing the M016-discovered gap where an already-correctly-committed pending journal entry could block `task-dispatch` indefinitely; `task-add <milestone-id> --file <yaml> --change-log <text>` (the sanctioned `confirmed`/`in_progress` task-insertion path, reusing the existing `task_amendment` journal operation unchanged, so review-staleness and auto-run invalidation apply for free); a completion-time usage warning for worktree-dispatched tasks; structured failure-evidence extraction (`summarizeFailure`, capped/truncated command output surfaced in `task-verify`'s own evidence); a clean CLI error boundary (`renderCliError`) with an actionable missing-`.pitway/`-state diagnostic; `.github/dependabot.yml` dependency configuration; a global `vitest.config.ts` `testTimeout`/`hookTimeout` increase, root-caused via the debugging skill to genuine concurrent-subprocess OS contention rather than a PitWay defect, closing the load-sensitive-tests Backlog item (`npm-pack.test.ts`'s own transient explicitly left open, since it was never a timeout-signature failure); a `protocol-driver.md` "Drafting write_scope" checklist codifying the write_scope/stale-test-assertion pattern's fourth recurrence as an installed, driver-facing rule rather than a lesson re-learned per milestone. Closed all eight of the Backlog's then-open, milestone-unassigned items in one pass. Zero standalone hotfix commits during its own execution. | self-hosted |
| M018 | Backlog / Deferred Work Capture: `pitway backlog add\|list\|show\|promote\|archive` — a root-level, schema v1, absent-tolerant `.pitway/backlog.yaml` as the single authoritative deferred-work store, replacing the now-superseded "Backlog / Pending Candidates" prose section this document had used to work around the gap since M012. Reuses the existing checkpoint-eligible journal machinery via one new `backlog_recording` operation type (never a new commit primitive); every mutation attaches to the active milestone *unconditionally*, no override — a real safety gap a pre-implementation `milestone-review` (architect role) caught and closed before any code was written (the original draft mirrored `task-amend`'s explicit-override-or-fallback pattern, unsafe here because `backlog.yaml` is the first journal-backed target not exclusively owned by one milestone's directory). `pitway resume` gains a read-only `pendingBacklogItems` view. Two task-level `write_scope` drafting-gap corrections (T005, via `task-amend`). Real dogfooding, not fabricated: four genuine backlog items (B001–B004) captured via the actual built CLI during this milestone's own execution, each a real developer request kept explicitly out of M018's own scope. Zero standalone hotfix commits during its own execution. | self-hosted |
| M019 | Milestone Merge Workflow and Backlog Promotion: `pitway milestone-merge <id> [--target <branch>]` — closes the manual-merge gap disclosed since M012, exercised by hand for M016/M018 before this; every state read resolves from the current branch before any checkout, refusing by name (never a blanket branch-name assertion) when the milestone's completion commit isn't reachable from current HEAD, so the legitimate idempotent re-run from the target branch post-merge still works; full git-safety gate set, each restoring the original branch on failure; ancestor-SHA idempotency surviving source-branch deletion; a new audit-trail-only `milestone_merge` journal kind. `ms-*` aliases for the 7 named milestone-* commands (B001) plus a `PitWay: ` description prefix on every owned command doc; a short display `label` per review role (B002); a documented backlog-storage-scalability investigation concluding no redesign is warranted at current or foreseeable scale (B003) — the first real exercise of `backlog promote`. A fourth item inserted directly by developer instruction: a driver-agnostic MUST-level runtime usage-propagation requirement (T007, run first), closing a real producer-side gap (the persistence/aggregation pipeline was already correct; no driver document had ever instructed extracting a dispatched worker's already-available usage figure). A pre-confirm `milestone-review` (architect + devops) caught and closed two real gaps before any code was written. Zero standalone hotfix commits during its own execution. Merged into `main` via its own new command, the first real, non-synthetic use of `milestone-merge`. | self-hosted |

\* **Self-hosting caveat (M004 AC014, disclosed):** M004 crossed the self-hosting boundary — created, confirmed, executed, verified, and completed through pitway commands — with **one disclosed exception**: commit `179491c` directly hand-edited T004's task definition in `tasks.yaml` because no supported task-amendment command existed. M004 is recorded as a self-hosting-boundary crossing with one disclosed manual task-definition amendment, not as fully self-hosted end-to-end. The gap is closed by M005's amendment command.

---

# Revised Roadmap (2026-08-22 — M005 through M019 delivered; M020 onward still proposed)

M005 through M017 were drafted, confirmed, executed, and verified against this roadmap; M018 onward remain proposed, confirmed one at a time as before. Sequencing principles (validated by delivery, not just proposed): the runtime journal (M005) existed before the interactive auto-run authorization that lives in it (M006, `auto-run` + `interactive-ux.md`, M006/T005); slug support (M005) existed before the first slugged directory (M006's own `M006-verification-hardening-claude`); the sequential MVP's own dogfood validation (M007) happened before its release (M008), exactly as the roadmap required — M007 gated M008, and M008 delivered the real, packaged, npm-installable binary that validation was ultimately for; M009 then closed two real, twice-observed lifecycle gaps M008's own post-completion exception surfaced (§ Bootstrap table, M008's row) and built the quick-change mechanism M007 had designed but deferred; M011 then delivered the Claude skills item M007 had left adopted-or-deferred, per an explicit 2026-08-19 developer directive overriding M007/AC009's own "revisit when a concrete use case exists" recommendation, with real comparative dogfood evidence recorded honestly (a negative/null single-trial result — see M011's own entry below); M012 then delivered the milestone-level Git branch strategy §15 had carried as a design sketch since M009/T006, with the range-bounded trailer lookup §10 had flagged as scheduled; M013 then delivered the PitWay Driver UX candidate M012/T009 had scheduled immediately after it — the permanent racing footer, the on-demand Progress Report, and a task-level `name` field, built entirely on data PitWay already tracked, with one disclosed deviation from the original candidate's wording (see M013's own roadmap entry below); M014 then delivered opt-in parallel task worktrees, layered on top of M012's branch-strategy pattern, with the dogfood irony that its own tasks were mutually parallel-ineligible under its own rules; M015 then delivered a first-class, role-based milestone review workflow from a developer-authored backlog doc, drafted immediately after M014 per that doc's own Timing section (see M015's own roadmap entry below); M016 then gated the parallelism increment with the first real parallel-worktree dispatch and the first real `git.branch_strategy: milestone` run, both proven live rather than only by synthetic test (see M016's own roadmap entry below); M017 then closed every Backlog item that had no roadmap slot assigned — including the structured failure-evidence extraction M007 had left adopted-or-deferred, now delivered for real (`summarizeFailure`, see M017's own roadmap entry below) — rather than continuing to let them accumulate as session-local unscheduled items; M018 then delivered the deferred-work backlog mechanism (`pitway backlog`) the now-superseded Backlog / Pending Candidates section had existed to work around, closing the discover → capture → defer → continue → promote gap (see M018's own roadmap entry above); M019 then combined the manual-merge gap disclosed since M012 (`pitway milestone-merge`) with the first real exercise of `backlog promote`, closing out three of M018's own genuinely dogfooded backlog items (B001–B003) into planned work rather than leaving them to accumulate unscheduled (see M019's own roadmap entry below). Branch/worktree parallelism (renumbered **M014–M015** by M012/T009, to make room for **PitWay Driver UX** at **M013**; the second of those two renumbered again to **M016** by M015/T011, to make room for **PitWay Milestone Review** at **M015**) is layered on top after release, each release increment gated by its own dogfood pass (**M016** gated the parallelism increment).

- **M005 — Workflow checkpoint semantics and hardening.** (Bare directory, grandfathered — slug support does not exist when M005 is created.) Scope: the revised Git checkpoint model — exactly three committed checkpoint kinds (milestone started/confirmed; task completed; milestone completed or cancelled). Task-transition writes (`ready`/`in_progress`/`review`, retries) and verification-result writes stay exactly as they are today — ordinary uncommitted `.pitway/` state-file writes that already fold into whatever commits next, no new machinery needed. What genuinely changes: usage recording and contract amendments — today's two standalone/ancillary-commit paths — plus the new task-amendment operation, are recorded through a **resumable, git-invisible runtime journal**, never as their own commit; this revises M004's AC008 ancillary usage-recording commits and standalone `milestone-confirm --amend` commits going forward — **M004's historical AC008 behavior and its actual commits are unchanged; it does not rewrite M004.** **M005 itself is the transition milestone: it is built and completed under the existing M004 checkpoint mechanics throughout its own task-completion commits — this model only becomes usable partway through M005 once the relevant task lands, and is authoritative starting with M006's own lifecycle, not retroactively true of M005's own earlier history.**

  **Task and contract amendments, corrected (2026-08-19 — the M005 scope originally drafted here said the amendment command "commits the amended artifacts itself," which contradicted the journal model in the same paragraph; that was wrong and is replaced by this):** an amendment (task-definition or contract) is validated and requires explicit developer approval, then recorded **durably in the runtime journal** — never as a standalone Git commit — carrying: an operation identity (so re-entry is idempotent/resumable), the approved hash it produced (contract amendments) or the validated field diff (task amendments), and Change-Log evidence. The journal entry must **survive process/session interruption** (it is durable local state, written before the operation is considered applied). **Every approved amendment is folded into the next checkpoint of any kind — normally the next task-completion checkpoint, otherwise the milestone terminal checkpoint** — never creating a commit of its own, and never depending on whether a task happened to already be in progress when the amendment occurred. On resume, if journal recovery is ambiguous (multiple candidate un-checkpointed amendments, or the approved hash/field diff cannot be reconstructed from the journal plus the last checkpoint), **execution blocks** and reports the ambiguity rather than guessing. No direct `tasks.yaml`/`contract.md` hand-edits, ever — including contract amendments: since a direct edit of `contract.md` is now itself prohibited, `milestone-confirm --amend` gains a **supported amendment-input path** (a validated draft file, or structured field changes passed as command input) rather than depending on an untracked hand-edit of authoritative state; developer approval and hash re-approval remain mandatory regardless of which input form is used.

  Also in scope: expected-PitWay-dirty vs unexpected-user-dirty working-tree classification; split of task `relevant_files` into **`context_files`** (files a worker may read) and hard-but-amendable **`write_scope`** (files a worker may create/modify — drives the dirty-subset check and completion staging), both additive-optional schema-v1 fields, with the complete field-combination rule table: **`relevant_files` only** — legacy behavior, unchanged (readable and writable). **`write_scope` only** — unrestricted task-relevant reads, bounded writes (`context_files` unset means no read restriction, `write_scope` is still the write/completion boundary). **`context_files` + `write_scope` together** — bounded reads *and* bounded writes; every path in `write_scope` must also appear in `context_files` (a task can never write somewhere it wasn't allowed to read) — a `write_scope` path absent from `context_files` is rejected at validation, naming the offending path. **`context_files` only** — rejected as incomplete: declaring readable paths without ever declaring the write boundary leaves completion staging undefined, so this combination fails validation rather than silently falling back to unrestricted writes. **`relevant_files` together with either `context_files` or `write_scope`** — rejected as ambiguous, naming the conflicting fields, never silently preferred. No task ever carries both an old-style and new-style scope declaration, and no task ever ends up with reads narrower than its writes. `write-ms-artifacts` (or equivalent) draft-artifact command supporting a "write artifacts only" confirmation path; **slugged-directory implementation** (derived once from the title, bare id canonical everywhere, resolution in the state store only, exactly-one-candidate rule, M001–M005 grandfathered bare — first slugged directory is M006's); a **roadmap-reconciliation task** verifying this plan against reality with tests; migration/compatibility rules keeping all M001–M004 history valid. Acceptance boundary: no interactive UI, no Claude asset changes, full suite green, existing history resolvable unchanged.
- **M006 — Verification hardening, Claude integration, and context efficiency.** (First slugged directory: `M006-verification-hardening-claude`; `M006` remains the sole canonical id — the slug is presentation/navigation metadata.) Delivered, in dependency order: **verification-execution hardening landed first** (T001-T002, §11) — a confirmed M005 defect (no per-check timeout, an unguarded recursion path), closed before anything else in this milestone relied on `pitway verify` staying safe. Then installable Claude assets + driver protocol (§9, T003); dependency-aware sequential dispatch with **one authoritative bounded bundle per task attempt** via `task-status <id> --context --json`, passed with a fixed wrapper — no driver re-planning, no duplicated per-task `PLAN.md` files, and an honestly-scoped claim: PitWay bounds only the *supplied* bundle, never a subagent's total context, and builds no runtime read-enforcement (§8); separation of main-driver instructions (`protocol-driver.md`) from bounded-worker instructions (`protocol-worker.md`); concise, capped worker reports (T003, `task-update`'s result-field cap); targeted tests during implementation with one full-suite run at the final gate; **arrow-key decision UX** (T005) owned entirely by the integration layer via the same milestone-approval/task-continuation prompts originally proposed, with auto-run authorization now a real, agent-agnostic CLI command (`auto-run enable|disable|status`, §7) rather than something the integration layer writes to state directly — invalidated on a hash change or a later contract/task amendment (both computed from the journal's own order) plus a set of live checks (unexpected dirty files, a verification failure needing a decision including a flaky-pass-after-timeout, missing manual/review evidence, ambiguity, a permission requirement, a merge conflict, a destructive action) that Core cannot precompute; **LSP usage delivered as pure driver guidance, not PitWay code** (`lsp-guidance.md`) — PitWay has no LSP detection/integration code of its own and does not manage a driver's tool availability; **measurable context-efficiency evidence** against the M004/M005 baseline (`docs/evidence/M006-context-efficiency.md`, T004), recorded honestly rather than favorably: raw per-task token cost went up, per-AC-of-contract-delivered cost went down, and a real, previously-unknown bundle-duplication defect was found and quantified (not fixed in M006 — carried forward). Acceptance boundary held: Core and the CLI stayed structured and non-interactive throughout.
- **M007 — Dogfood validation and Adaptive Workflow Intensity decision.** Delivered, 13 tasks: real-history dogfood evidence (fresh-session resume regression test, a Git traceability audit proving generic classification against synthetic history — never hardcoding this repository's real SHAs — confirming 7 standalone hotfix commits across M005+M006 plus one between-milestone maintenance commit (`85fa243`) correctly classified separately, and a new finding: `backup/pre-rewrite-main` has zero common ancestor with `main`, a real early-history rewrite); three architectural findings evaluated against that evidence and M005's own history (contract-fixed/task-adaptive: adopt modified — documented tool-selection guidance only, approval gate unchanged; write-scope boundary shape: reject the glob/denylist alternative, 4.2% measured amendment rate doesn't justify it; ripple-fix policy: adopt modified — approved fixes fold into the discovering task's own commit when directly causal/bounded/tested, gated by an approved write_scope task-amend first); a full quick-change workflow design (deferred, later refined post-completion to center on post-milestone manual bug findings as the primary use case, with an interim policy now in effect: use a one-task corrective milestone rather than further unattributed standalone hotfix commits); explicit Claude-skills adoption evaluated (deferred — zero real usage exists to justify building it); right-sized dispatch mode adopted (inline is the default for docs/review/small-scope work, tdd strategy alone never forces sub-agent dispatch, dispatch is reserved for material isolation/concurrency benefit weighed against ~32K-token measured startup overhead) and formalized into `protocol-driver.md`/`dispatch.md`; the M006/AC005 context-bundle duplication defect fixed for real (`mapped_ac_ids`, additive-optional — M007's own already-confirmed tasks cannot use it themselves, a disclosed non-bootstrap gap; real use starts with M008's contract-drafting); structured failure-evidence extraction adopted as a future direction with binding design constraints, implementation deferred; a first-class tooling-defect recovery procedure documented against four real worked examples; a completed-task-revision path proven as a real, tested mechanism using only already-shipped commands, zero new Core code; and the dispatch/worker protocol hardened with two rules discovered live during this milestone's own execution — a pre-dispatch conflict-preflight check (a real dispatch had to be stopped and corrected mid-flight) and a tiered Decision Authority Policy (autonomous / continue-and-batch-report / mandatory developer gate) governing how much confirmation a driver decision requires during an auto-run. One gap remains genuinely undecided and disclosed, not resolved by any M007 task: whether/how to build actual read-boundary enforcement for a dispatched worker (M006/AC004 deferred this "to an M007 decision"; no M007 AC addressed it) — explicitly assigned to M008's release-readiness contract (§8), not silently carried forward without a disposition.
- **M008 — README, packaging, and release readiness.** Delivered, 5 tasks: a real `tsc`-based build (`tsconfig.build.json`, separate from the untouched `noEmit` `tsconfig.json`) closing the M004/T007 finding for real — Node's native TS loader never remapped this repo's `.js` import specifiers to `.ts` sources, so the old direct-`.ts` `bin` failed with `ERR_MODULE_NOT_FOUND`; a cross-platform, recursively-discovering asset-copy script ships the Claude Markdown assets alongside the compiled code (`tsc` alone does not copy them); a real subprocess spawn of the compiled `dist/` binary individually confirms all 14 commands. Complete, explicit npm packaging metadata (`name`/`version`/`files`/`license`/`author`/`repository`/`bugs`/`homepage`, `main`/`exports` deliberately omitted — CLI-only), validated by a real `npm pack` → fresh-temp-project-install → real-installed-binary check, not just the unpacked `dist/` tree — which caught and fixed a real, previously-unknown defect: the CLI's entry-point guard compared `process.argv[1]` against `import.meta.url` with strict equality, which never matches when Node invokes the binary through npm's own generated symlink (the exact mechanism every real install uses), so every command silently no-op'd once actually installed; fixed with `realpathSync`, folded into the discovering task's own commit per the M007/AC004 ripple-fix policy. The worker read-boundary enforcement question M007 left genuinely undecided is resolved (§8: advisory-only, permanent-until-revisited). A real, honest `README.md` exists, bounded only by clone-durable evidence. This milestone establishes release readiness only — **`npm publish` has not run**; publishing remains a separate, explicitly developer-approved external action.
- **M009 — Lifecycle Corrections and Quick Change.** Delivered, 6 tasks: two real, twice-observed gaps in PitWay's own lifecycle closed for good — `milestone-add --replace` (draft-only, same-id, no-git-operation in-place correction, replacing the ad hoc git-restore-and-relocate workaround M008's own draft correction needed) alongside `milestone-cancel` (the separate preserve-and-retire path, id never reused), and `verification-repair` (a real, two-phase, approve-before-edit, VR-id-identified, dual-trailer mechanism for the tasks-complete/pre-`milestone-complete` window M008's `6f8b5e6`/`ed119ed` standalone commits had occupied, gated on an explicit approved scope rather than task `write_scope` membership, reusing `pitway verify`'s own check-execution path rather than duplicating it). The quick-change workflow M007 designed but left unbuilt is now real, deliberately split across three dependency-ordered tasks (state/schema/lifecycle; verification/commit/recovery/trailer; CLI/resume/assets/integration) rather than landed as one oversized task — `active_milestone: null` and a clean working tree required at create, every `.pitway/` path protected from its declared scope, append-only run history, `PitWay-Change`-trailer commit identity, and `pitway resume` as the authoritative recovery view for a pending change (never requiring a separate `quick-change status` call to discover one). Proceeds ahead of quick-change's own M007-stated revisit threshold (>= 3 real cases; one, `85fa243`, qualified) as an explicit developer decision. Zero standalone hotfix commits during its own execution (matching M007's own record) — every ripple effect a task's own approved change caused in an out-of-scope file (three baseline-path test arrays after `verification-repair`'s AC002 work; an `isAutoRunAuthorized` type narrowing broken by quick-change's own milestone-less journal schema; a `resume.test.ts` fixture missing `git init` once `resume` started reading the journal) was folded into the discovering task's own commit via an approved `write_scope` `task-amend` first, per the M007/AC004 ripple-fix policy. This same reconciliation task also recorded an independently reviewed token-accounting feasibility conclusion (`docs/evidence/M009/token-accounting-feasibility.md`) and the M010/M011/Token-Telemetry-Spike roadmap entries below.
- **M010 — Worker Verification Evidence.** Delivered, 4 tasks: eliminates routine duplicate driver test runs without weakening verification. A new PitWay-owned `task-verify <id> [--typecheck <command>]` command executes exactly an `in_progress` task's own approved `command`/`tdd` verification command (reusing the existing `executeCommand` primitive, never a new subprocess mechanism), capturing the exact command, exit code, best-effort pass/fail counts, duration, termination reason, and an optional typecheck result. The fingerprint covers exactly the task's declared `write_scope`/`relevant_files` — never `.pitway/` workflow state — settled only after two architecture-review CRITICAL findings ("every dirty path" and then a workflow-state-inclusive dirty-path check both self-invalidated against the mandatory `in_progress → review` `tasks.yaml` rewrite between verify time and completion time) converged on a dirty-path allowance built to exactly mirror `completeTask`'s own already-shipped `expectedPaths` construction (declared scope + the milestone's own `tasks.yaml` + `classifyDirtyPaths`'s real journal-backed expected paths), never a broad `.pitway/**` allowance. Each declared path fingerprints as a real content hash or a fixed missing-file marker; a rename requires both the old and new path explicitly declared (old → missing, new → content hash) — an undeclared half is caught by the same dirty-path allowance with a diagnostic naming the specific path, no new rename-pairing logic added (git never reports an unstaged rename as a single paired entry). A declared git-ignored path is refused outright via a real `git check-ignore` call. Evidence persists as a git-invisible, resumable `task_verify_evidence` journal record under a unique evidence id. `task-update`'s completion path resolves it implicitly (the newest record for the milestone+task by append order) or via an explicit `--evidence <id>`, validating task identity, run success, attempt, exact command-string identity, `write_scope` as a normalized set, and a freshly recomputed fingerprint — any one mismatch refuses completion outright naming the specific mismatch, never falling back to an older matching record; no record at all for a task falls through byte-for-byte unchanged to the pre-existing `--result`/`--message` path. `--result`/`--message` remain required inputs, unchanged; when evidence is resolved it unconditionally becomes the authoritative `result.evidence`, never overwritten by driver-supplied text. A real end-to-end integration test drives a task through the full `in_progress → task-verify → review → completed` lifecycle via the actual CLI as the direct regression proof that `review`'s own `tasks.yaml` rewrite never invalidates recorded evidence. The driver still independently reviews the diff and `write_scope` first, every time — `task-verify` formalizes and replaces the ad hoc rerun, it does not replace that review. Full typecheck and the full suite remain milestone-level gates only, never per-task; only the capped evidence fields land in `task.result.evidence`, never full logs. No usage-attachment mechanism was added — whole-run/main-agent token accounting remains the separate, already-deferred Token Telemetry Spike investigation below, untouched by this milestone.
- **M011 — Claude Skills.** Delivered, 8 tasks, per an explicit 2026-08-19 developer directive overriding M007/AC009's own "revisit when a concrete use case exists, not on a fixed schedule" recommendation. Six skills (`debugging`, `bug-fix`, `testing`, `code-quality-review`, `architecture-review`, `security-audit`) vendored verbatim, byte-for-byte, from `github.com/thixpin/claude-config` (the developer's own MIT-licensed repository) at a pinned commit, with a `NOTICE.md` attribution file recording the source, commit, license, and the copyright notice copied verbatim from the pinned commit's own `LICENSE`; `infra-design`/`terraform-review` explicitly evaluated and rejected (no PitWay infrastructure/IaC domain). Installed through the existing Claude-assets installer with zero installer-discovery code changes, confirming M007/T008's own prediction. A corrected, per-file content-comparing `.claude/` classification (`classifyClaudeAssets`) replaces the former coarse present/missing/invalid probe, refusing only on an actual byte conflict rather than any partial mix. Two new root files, `AGENTS.md`/`CLAUDE.md`, make PitWay usage discoverable to any agent opening an initialized project, installed non-destructively (created/no-op/preserved-with-warning) and wired into `milestone-confirm`'s baseline staging content-awarely. A shared classification mechanism (`listSafeManagedDirtyPaths`) closed two further, real, pre-existing gaps for good: `quick-change create`/`commit` now succeed immediately after a fresh `init` when the only dirty paths are content-verified PitWay-managed output, and `milestone-confirm`'s own baseline staging became content-aware for `.claude/` assets too (a gap that had existed since M006). A new, additive-optional `required_skills: string[]` task field gates `task-status <id> --context` through a strict Core/State-layered pre-dispatch context gate — proving PitWay's own managed skill installation is present, never that a dispatching harness actually loads it. Real comparative dogfood evidence (`docs/evidence/M011/skill-dogfood-evidence.md`) was recorded honestly: a rubric written before either of two isolated, fresh-context sub-agent dispatches ran, with the skill-loaded run's loading independently verified via a byte-for-byte-matching verbatim quote from the real skill file — result was a **negative/null single-trial finding** (both runs found the same real structural issues; the baseline scored marginally better on two of four rubric items; the skill-loaded run used meaningfully fewer tokens), reported candidly per M006/AC005's own precedent rather than reframed. Same disclosed non-bootstrap gap as M007/M008: M011's own tasks.yaml was confirmed before `required_skills` existed in this repository's own validation code, so no M011 task could declare it on itself.
- **Token Telemetry Spike** (unnumbered, time-boxed — never consumes a milestone number, never shifts the M013/M014/M015/M016 renumbering below, positioned after M011, ahead of M012). Recorded by M009/T006 alongside the independently reviewed token-accounting feasibility conclusion at `docs/evidence/M009/token-accounting-feasibility.md`: feasible only partially; PitWay's exact supported data is four token dimensions; the real session partition is main + subagent + auxiliary, not main + subagent; inline-main-task and orchestration-overhead values are PitWay-derived and conditional, not directly measured; stable per-subagent-instance attribution requires beta traces; multi-session totals are partial segment accumulation, never exact; OpenTelemetry-based tracing is opt-in and operationally expensive; transcript parsing, TUI scraping, snapshot accumulation, and an Agent SDK architectural inversion (which would itself violate this project's own "Core must never import AI-provider code" constraint) are each evaluated and rejected; any value that is unknown or incomplete stays `null`/N/A, never estimated, unchanged from decision 8. Scoped as exactly seven experiments, E0–E6, each targeting one load-bearing join the feasibility conclusion identifies: E0 confirms the four supported dimensions against real captured data; E1 validates the three-way main/subagent/auxiliary session partition; E2 validates that inline-main-task/orchestration-overhead values are correctly labeled derived-and-conditional rather than measured; E3 probes whether stable per-subagent-instance attribution is achievable without beta traces and quantifies the gap if not; E4 measures how far multi-session partial accumulation drifts from ground truth; E5 measures OpenTelemetry's real opt-in operational cost against its accuracy gain; E6 re-confirms, rather than assumes, that the four rejected alternative approaches still fail to meet the bar. A full **Usage Accounting** milestone becomes a real candidate for scheduling only if the spike validates every one of those seven joins exactly; if any fails to validate exactly, the limitation is documented permanently in the same evidence file and the idea is deferred indefinitely, not retried on a fixed schedule.
- **M012 — Milestone Git branch isolation.** Delivered, 9 tasks: `git.branch_strategy: main | milestone` config policy (the first real, non-`schema_version` field ever added to `config.yaml`); a fresh `milestone-confirm` under `milestone` strategy creates one deterministically-named branch (`pitway/<id>-<slug>`) after its existing dirty-tree safety check passes and records `base_branch`/`base_revision` in contract frontmatter, refusing outright (never reusing or resetting) if that branch already exists; a resumed confirm performs no branch mutation at all, refusing on any position mismatch instead; a shared guard (`assertOnMilestoneBranch`) wired into task completion and `milestone-complete` refuses the same way before committing, never auto-checking-out to fix a mismatch; `pitway resume` surfaces a tracked branch and any mismatch, read-only; range-bounded trailer lookup (`resolveCommitSha`'s new `since` parameter) throws loudly on an unreachable `base_revision` rather than silently widening to an unbounded scan; `milestone-complete` reaches a verified merge-ready/PR-ready state (proven by concrete git-plumbing assertions, not asserted in prose) without ever performing an automatic merge, force-push, or branch deletion. `main` strategy (explicit or absent) is completely unaffected in every respect — proven by a dedicated regression pass alongside the real end-to-end lifecycle test. A `protocol-driver.md` "Verification discipline" section was added per an explicit developer directive given during M011's own execution. Disclosed, real limitation found during delivery (T007): switching all the way back to the true pre-milestone base branch (not a sibling branch) leaves `.pitway/state.yaml` and the milestone's own directory missing from the working tree entirely — committed content unique to the milestone branch until it is eventually merged — so every `pitway` command fails with a plain file-not-found error there rather than a graceful mismatch report; a pre-existing, out-of-scope limitation, not something this milestone's own mismatch-detection was expected to cover. Disclosed non-bootstrap gap: M012's own development happened under `branch_strategy: main`, since the `milestone` strategy this milestone builds did not exist yet at its own confirm time. Zero standalone hotfix commits during its own execution; one task-level write_scope correction (T005, via `task-amend`, developer-approved) after a drafting gap between AC005's own text and the task's declared scope was discovered mid-execution.
- **M013 — PitWay Driver UX** (delivered; inserted at this slot resolving a numbering collision flagged when M012 was drafted — see M012/T009): the exact five-band cumulative workload formula already in informal driver use, formalized as a pure, tested Core function (`computeWorkloadPercentage`); a permanent one-line racing footer (`🏎️ ~<workload>% · ✅ <completed>/<total> · Next: <task/gate>`), wired into `resume` and `milestone-status`, `null` before confirmation, with a `🏁` icon shared between "verification-only-remains" and "completed" (distinguished by trailing `Next: <gate>` vs. `Complete` text); an on-demand, fully structured Progress Report (`milestone-status --report`) covering workload, token totals, a task table with evidence-honest labeling, the critical path (a new pure `computeCriticalPath` function — longest remaining dependency chain), and active/next task; a new read-only `verify --status` view, and the shared verification-status helper (`computeLatestCheckResults`/`allChecksPassed`) it introduced, extracted from `milestone-complete`'s own former private gate check with zero behavior change and reused by the workload/footer/report functions above; a new, additive-optional `taskSchema.name` field (short label, id-fallback, mirroring `mapped_ac_ids`'/`required_skills`' own bootstrap-gap precedent — this milestone's own tasks cannot carry one). **Disclosed deviation**: the original candidate described the token breakdown as covering a "driver overhead" dimension; `docs/evidence/M009/token-accounting-feasibility.md` flags orchestration overhead as not directly measured, only derivable, pending the not-yet-run Token Telemetry Spike — the delivered breakdown reports only the directly-measured task/planning/qa categories plus total and missing-usage count, with no fabricated figure. Zero standalone hotfix commits during its own execution.
- **M014 — Parallel task worktrees** (delivered, 11 tasks; title shortened from "Parallel task worktrees and deterministic integration" at draft-review time; renumbered from M013 by M012/T009, itself renumbered from M010 by M009/T006): opt-in `execution.strategy: sequential | parallel_worktrees` config (absent ⇒ sequential, byte-identical, proven by a full-lifecycle equivalence test — M012's `branch_strategy` pattern reused exactly); a pure eligibility function (`checkParallelEligibility` — candidate `ready`, no transitive dependency in either direction, pairwise-disjoint `write_scope` by exact path comparison, against ALL `in_progress` tasks, inline ones included; legacy `relevant_files` tasks never eligible); a Git-layer worktree module (one temporary worktree per dispatched task at `.pitway-worktrees/<mId>-<tId>/` on scaffolding branch `pitway/task/<mId>-<tId>`, refuse-on-collision, both the directory prefix and the runtime marker excluded via `.git/info/exclude` so removal never needs `--force`); `task-dispatch` (strategy/eligibility/dirty-tree/pending-amendment gates, then transition → journal record → worktree, with the `--json` handoff envelope deliberately excluding the context bundle — the driver passes the bundle from the authoritative main root); a fail-closed CLI state guard (every command refuses inside a task worktree except an enumerated read-only allowlist, flag-aware for `verify`; `init`/`write-ms-artifacts`/future commands covered structurally); `task-integrate` (diff-apply, never merge: pinned `--binary --no-renames` range diff scope-validated before any write incl. deletions and `.pitway/` protection, pre-checked apply landing uncommitted, `worktree_integrate` journal record carrying the worker SHA as evidence-only metadata, worktree/branch cleanup; idempotent re-runs across both crash windows; completion unchanged — authoritative main-tree `task-verify` → `review` → `completed` atomic commit); `task-discard` (the abandonment exit: record → cleanup → `failed`, re-dispatch via `failed → ready`; a live-dispatched task refuses all direct `task-update` status changes until closed); `resume` residue classification (four read-only classes: vanished-worktree, recordless-worktree, cleanup-pending, inline-or-interrupted); an end-to-end lifecycle test proving mainline history after parallel execution is structurally indistinguishable from sequential (per-task trailer commits in integration order, zero merges, zero surviving scaffolding, state equal to a real sequential run) plus the conflict and `branch_strategy: milestone` interaction cases; protocol-doc guidance and three new command docs (§9). Three previously-binding positions explicitly re-dispositioned — see the revision header: (1) sequential-MVP wording (sequential stays default), (2) the per-task-branch principle (persistence is the invariant), (3) the atomic-commit invariant (preserved, sibling-rider nuance disclosed) — plus deviation (4): the original entry's worker "returns a verified result" is refined to advisory worker checks + authoritative post-integration main-tree `task-verify`, since worktree-written evidence resolves per-worktree and can never reach the authoritative journal, and only a post-integration run observes the landed state. Dogfood irony, disclosed: M014's own tasks share `schemas.ts`/`journal.ts`/`cli/index.ts` write scopes and were therefore mutually parallel-ineligible under M014's own rules — this milestone built parallel execution while itself executing sequentially; the first real parallel dispatch belongs to a later milestone (M016's validation pass is the natural candidate). Two standalone developer-directed fixes landed between tasks during execution (racing-footer task-name segment + footer output-separation test contract — M013-surface polish, outside M014's scope, each explicitly requested and committed on a clean tree between tasks).
- **M015 — PitWay Milestone Review** (delivered, 12 tasks; drafted from the developer-authored backlog doc `drafts/M015-milestone-review-backlog.md`, immediately after M014 per that doc's own Timing section, displacing the previously planned "Extended dogfood validation + release increment" to **M016** — see that entry below): a first-class, role-based milestone review workflow layered entirely on top of existing mechanics — reviewers produce findings only, never mutate or confirm the milestone directly. `reviewsFileSchema` (schema v1, `reviews.yaml` per milestone, absent-file-tolerant like `verification-repairs.yaml`, created only on first write, never by `milestone-add`); a nine-role Core registry (`developer`, `architect`, `devops`, `qa`, `product`, `business`, `business-analyst`, `ui-ux`, `user` — the backlog doc's original six plus three developer-requested additions before confirmation, each focus deliberately disjoint from its nearest neighbor); `milestone-review start|brief|record|report|decide` (§7; surface 21 → 22), each pure state/validation — dispatching reviewer subagents and reconciling their findings stays the driver's job (`commands/milestone-review.md`, §9). A definition-hash staleness gate pins a canonical projection of the contract's own CONTENT (id/title/requirement/acceptance_criteria/verification + body) plus every task's DEFINITION at session start, deliberately excluding every execution/lifecycle field on both sides (contract: status/confirmed_at/verification_approved_hash/base_branch/base_revision; task: status/attempts/result/usage) — a real revision (`task-amend`/`--replace`/`--amend`) invalidates a session; task transitions and `milestone-confirm`'s own status promotion never do. Every review mutation is journal-backed exactly like a usage recording or amendment (`review_recording`, a fourth entry-kind operation type sharing `usage_recording`'s own checkpoint mechanics), so the four existing task-flow dirty-tree checks expect a materialized-but-uncommitted `reviews.yaml` with zero call-site changes; exactly two enumerated per-file allowlists (baseline, completion) gain a `reviews.yaml` row. Interactive role selection is a plain `node:readline` numbered multi-select against injectable streams — a disclosed deviation from `interactive-ux.md`'s arrow-key design, which stays design-only, adding no new runtime dependency either way. `milestone-confirm`/`milestone-complete` stay deliberately uncoupled from review sessions in MVP (no open-session check at either) — the existing human gate already owns that judgment, a disclosed decision. Two independent pre-confirmation reviews (Senior SWE + Senior Solution Architect — the same informal M014-style two-reviewer flow the backlog doc's own Scope Notes sanction as dogfooding evidence) caught real defects folded into the draft before confirmation: the journal wiring's missing baseline/completion allowlists, the content-hash's staleness scope, and the report's Core-never-reconciles honesty text. One genuine gate-caught defect, corrected via the same widening precedent M012/M013/M014 established: `content_hash`'s contract-side component was initially implemented as raw `contract.md` file bytes, so `milestone-confirm`'s own status/confirmed_at/verification_approved_hash rewrite staled every session opened before confirm, directly contradicting this milestone's own design decision 4 — corrected to the content-only projection above once T008's own real-lifecycle test caught the contradiction, with the contract text and this roadmap entry both reflecting the fix rather than the original drafted design. A second, smaller gate-caught fix followed from the first: `tests/unit/git-baseline.test.ts`'s exact-path assertion for `computeExpectedBaselinePaths` didn't know about the new `reviews.yaml` row, caught by the same full-suite run and fixed the same way. One drafting-gap fix, unrelated to product code: this document's own T012 task named a "CT016" review check that had never actually been added to the contract's `verification:` array — added via `milestone-confirm --amend`, mirroring the existing CT014/CT015 review-check style. Bootstrap disclosure: this milestone's own pre-confirmation review necessarily used the informal two-reviewer flow above, not the `milestone-review` command it delivers — the first real `pitway milestone-review` run belongs to M016's own drafting.
- **M016 — Extended Dogfood Validation and Release Readiness** (delivered, 5 tasks; renumbered from M015 by M015/T011, to make room for **PitWay Milestone Review** at **M015**; itself renumbered from M014 by M012/T009, itself renumbered from M011 by M009/T006): the first real dogfooding of both M012's branch-strategy and M014's parallel-worktree mechanisms in this repository's own history. Two real, disjoint-scope, backlog-sourced fixes — hardening the three-milestone-recurring nested-full-suite flake, and fixing the M011 `QuickChangeError`/`requireQuickChange` dogfood findings — dispatched and integrated as the **first real parallel dispatch** since M014 built the mechanism (real `worktree_dispatch`/`worktree_integrate` journal records, mainline history proven structurally indistinguishable from sequential — per-task trailer commits, zero merges, zero surviving scaffolding). The flake's root cause: Vitest's default 5000ms per-test timeout too tight for the test's real ~15-CLI-invocation, real-git-subprocess workload under genuine concurrent load — confirmed by a real reproduction (two concurrent full suite runs), never a PitWay defect; hardened with a disclosed, evidence-backed timeout increase matching existing precedent. The `QuickChangeError` class-identity split (two textually-identical but distinct classes across `create.ts`/`run.ts`) and the `requireQuickChange` duplication (four files, one more than the original M011 finding named) both fixed, with new regression tests covering all three importing modules including `promote.ts`. This milestone itself confirmed under a real `git.branch_strategy: milestone` opt-in — its own dedicated branch, `pitway/M016-extended-dogfood-validation-and-release`, for its full lifecycle. Context-efficiency evidence recorded, confirming the M007/AC011 `mapped_ac_ids` bundle-filtering fix works correctly on live dispatch data (a task's bundle now carries only its own mapped AC, never the contract's full AC array) — correcting this milestone's own contract, which assumed a historical post-fix re-measurement that checking the record found was never actually performed. The first real `pitway milestone-review` run belonged to this milestone's own drafting, per M015's own disclosed handoff — a QA-role review caught a real blocker (AC003's own verification check could never distinguish a real fix from no fix, since a standalone single-file run always passes) and a major gap (AC007's documentation edits had zero verification coverage beyond the shared test/typecheck gate), both fixed before confirmation. Two genuine, previously-undiscovered defects found live during execution, both disclosed rather than silently absorbed: (1) `milestone-confirm`'s baseline commit never calls `reconcilePending`, so journal-pending entries whose target file it already committed correctly can block `task-dispatch` indefinitely — discovered attempting this milestone's own first parallel dispatch (the milestone's own pre-confirmation QA review left 3 such entries pending), worked around procedurally (a fresh confirm with no pending entries, rather than a code fix) rather than fixed in `src/`, left as a fresh backlog candidate; (2) once a confirmed milestone's task graph is fixed, no sanctioned command exists to add a new task or to cancel/undo the confirmation itself — discovered when an originally-planned dedicated warm-up task for defect (1) proved impossible to add post-confirm, and a subsequent attempt to cleanly undo the confirmation via `git reset` proved incomplete on its own (the git-invisible runtime journal does not reset with the commit history), requiring manual out-of-architecture journal-file surgery to actually recover a clean state — both real constraints, neither previously documented, now recorded here and as fresh backlog candidates for a future milestone to actually address in `src/`.
- **M017 — Backlog Closure and Lifecycle Hardening** (delivered, 9 tasks): closed every Backlog item that had accumulated with no roadmap slot, all found live during prior milestones' own execution. `reconcilePending(root, milestoneId)` now runs after `createBaselineCommit` in both of `milestone-confirm`'s commit-producing paths, fixing the M016-discovered gap that could block `task-dispatch` indefinitely after a pre-confirmation review session. `task-add <milestone-id> --file <yaml> --change-log <text>` is the new sanctioned mid-flight task-insertion path for a `confirmed`/`in_progress` milestone — the decision made was insertion-yes/cancel-no: `milestone-cancel` deliberately stays draft-only (no new state-machine edge), with `protocol-driver.md`/`commands/milestone-cancel.md` now documenting that a confirmed milestone's remaining work is abandoned by cancelling not-yet-started tasks and completing, never `git reset`. `task-update <id> completed` now emits a one-line stderr usage warning (plus an additive `--json` key) when the task was worktree-dispatched but received no `--usage` — detection only, chosen over a new `usage-correct` command, disclosed as blind to inline sub-agent dispatches. Failed `command`-type verification evidence now carries a structured `failures: ...` summary (`summarizeFailure`, up to five Vitest-marker/error lines, capped at 40% of the evidence budget) ahead of the existing tail-trimmed output — the item M007 had left adopted-or-deferred, delivered for real. `src/cli/errors.ts` gives the real binary a clean error boundary (a PitWay refusal prints its message only; a builtin/unexpected error keeps its stack) and maps a `StateStoreError` whose cause is ENOENT on `.pitway/state.yaml` to an actionable "run `pitway init`, or check out the right branch" message, replacing what had been an unhandled-rejection stack dump — the M013-deferred diagnostic, delivered for real. `.github/dependabot.yml` (weekly npm updates, grouped dev-dependencies, explicitly no auto-merge) plus a README "Maintenance" note. A global `vitest.config.ts` `testTimeout`/`hookTimeout: 60000` replaces the one-off per-test override `completed-task-revision-path.test.ts` carried alone, extending M016/T001's fix to the other real-git-subprocess tests the same induced-load reproduction found timing out under load (`milestone-complete.test.ts`, `verification-repair.test.ts`, `execution-strategy.test.ts`, `review-state-lifecycle.test.ts`, `branch-isolation-lifecycle.test.ts`) — disclosed as never claiming to fix the separately-observed `npm-pack.test.ts` transient, which was not a timeout-signature failure and stays an open, unassigned item. `protocol-driver.md` gains a "Drafting write_scope" checklist (the concrete assertion sites to grep before finalizing a task that changes a count/enumerated-list/persisted-string shape) directly addressing the four-times-recurring pattern the Backlog had named. A two-role (QA + architect) pre-confirmation `milestone-review` folded 16 findings into the drafted contract/tasks before confirmation — the second real use of the command, after M016's own first. Zero standalone hotfix commits during its own execution; one disclosed process note (T007's own completion evidence is a direct `npm test` run, not a formal `task-verify` record, after a sequencing slip moved the task to `review` before `task-verify` could run — caught and disclosed rather than silently mislabeled as verify-backed).
- **M018 — Backlog / Deferred Work Capture** (delivered, 6 tasks): a controlled mechanism for capturing work discovered mid-task that is intentionally out of scope, without ever silently expanding the current task or milestone's own scope — closing the discover → capture → defer → continue → promote gap. `.pitway/backlog.yaml` (root-level, schema v1, absent-tolerant) is the single authoritative store; `pitway backlog add|list|show|promote|archive` follows the existing `quick-change` parent/subcommand template. Reuses the existing checkpoint-eligible journal machinery via one new `backlog_recording` operation type (never a new commit primitive, never a dedicated commit) — `classifyDirtyPaths`/`reconcilePending` needed zero further changes beyond one new `resolveTargetPath` case, verified directly before drafting rather than assumed. A pre-implementation `milestone-review` (architect role) caught the contract's original design mirroring `task-amend`'s explicit-override-or-fallback pattern for journal attachment too — unsafe specifically here, since `.pitway/backlog.yaml` is the first journal-backed target not exclusively owned by one milestone's directory, so an override could misattribute a pending entry to a retired milestone that would never checkpoint it. The contract was revised before any code was written: every backlog mutation now attaches to the active milestone *unconditionally*, with no override at all; `add`'s `--milestone`/`--task` became pure `source` annotation (defaulting to the active milestone), and `promote`'s became the promotion target only, closing the flag-overloading the same review flagged. `promote` never creates the task/milestone itself — a pure terminal transition linking an already-drafted piece of planned work back to its backlog origin, mirroring `promoteQuickChange`'s identical boundary. `pitway resume` gains a read-only `pendingBacklogItems` view. Two task-level `write_scope` drafting-gap corrections (T005, via `task-amend`): one path named `.claude/protocol-driver.md` instead of its real, tracked source; a second named `.claude/commands/backlog.md` itself, which `task-verify` correctly refuses as a git-ignored declared path. Real dogfooding proof, not fabricated: four genuine backlog items (B001–B004) were captured via the real, freshly-built CLI during this milestone's own execution — deferred developer requests (milestone command discoverability/`ms-*` aliases; reviewer-selection UX formatting; backlog storage scalability at scale; CLI test-coverage investigation) that arrived mid-flight and were explicitly kept out of this milestone's own scope rather than absorbed. Zero standalone hotfix commits during its own execution.
- **M019 — Milestone Merge Workflow and Backlog Promotion** (delivered, 7 tasks): `pitway milestone-merge <id> [--target <branch>]` closes the manual-merge gap disclosed since M012 and exercised by hand for M016/M018 — every state read resolves from the current branch before any checkout, refusing by name (never a blanket branch-name assertion) when the milestone's completion commit isn't reachable from current HEAD, so the legitimate idempotent re-run from the target branch post-merge still works; full git-safety gate set (dirty tree, missing target/source branch, an in-progress merge, a real conflict, each restoring the original branch on any failure); ancestor-SHA idempotency surviving source-branch deletion; recorded as a new audit-trail-only `milestone_merge` journal kind. A pre-confirm `milestone-review` (architect + devops) caught and closed two real gaps before any code was written — the wrong-branch refusal and a missing source-branch-existence check — both landing as ACs, not afterthoughts. Promotes three of M018's own genuinely dogfooded backlog items into planned work, the first real exercise of `backlog promote`: B001 (`ms-*` aliases for the 7 named milestone-* commands, plus a `PitWay: ` description prefix on every owned command doc, skills excluded) and B002 (a short display `label` per review role, rendered `RoleName: label`, `focus` left untouched) as CLI/UX fixes; B003 (backlog storage scalability) as a documented investigation concluding no redesign is warranted at current or foreseeable scale. A fourth item, inserted directly by developer instruction rather than promoted from the backlog: a driver-agnostic MUST-level runtime usage-propagation requirement (T007, run first) — closing a real gap a dedicated investigation found in the *producer* side only (the persistence/aggregation pipeline was already correct; no driver document had ever instructed extracting a dispatched worker's already-available usage figure), formalized in `IMPLEMENTATION_PLAN.md` §8/§12 as provider-agnostic (so a future adapter inherits the same obligation) and concretely in `dispatch.md`/`protocol-driver.md`. Several real findings surfaced during execution, each disclosed rather than smoothed over: a pre-existing, uncommitted developer edit to `docs/assets/workflow.mmd`/`.svg` blocked the baseline commit and was folded into T006 by explicit developer decision; a dispatched worker's turn once ended mid-run with an unfinished backgrounded test command (never valid completion evidence per protocol) and was resumed rather than trusted; T003's own `write_scope` was drafted as a bare directory path, which `checkWriteScope`'s exact-path `Set` matching doesn't support — caught and fixed via `task-amend`, the same class of error M018/T005 hit; and a 391-second verification hang reproduced as transient system resource contention from `build-bin.test.ts`'s real binary builds, not a code defect, confirmed by two clean re-runs before completing. Zero standalone hotfix commits during its own execution.

Milestones remain proposed-and-confirmed one at a time through the normal contract flow; later boundaries may be re-cut at each milestone boundary as evidence accumulates.

## Backlog / Pending Candidates

Real, evidence-backed items raised during past milestones' own execution that no current milestone owns and no roadmap slot has been assigned to. Recorded here (added by M012/T009, per an explicit developer request) so they live in the project's own durable documentation rather than only in session-local notes; each stays here until either scheduled into a real milestone/`quick-change` or explicitly retired with a reason.

**Superseded by M018.** Every entry below is historical — struck through and marked DELIVERED, the last one closed out by M017. Going forward, new deferred-work items are captured via `pitway backlog add` and live in `.pitway/backlog.yaml`; `pitway backlog list` is the current, authoritative source, not this section. The entries below remain as a frozen historical record and are never retroactively migrated into `.pitway/backlog.yaml` or deleted.

- ~~**Structured failure-evidence extraction**~~ **DELIVERED by M017/T004** (raised M007). `summarizeFailure(output, budget)` (`src/core/verification/failure-summary.ts`) extracts up to five failure lines — Vitest's `FAIL <file> > <name>`/`×` markers plus the first `AssertionError:`/`Error:` lines, with a runner-agnostic `/\b(FAIL|ERR|Error)\b/` fallback when neither matches — each line capped at 200 chars, the whole summary capped at 40% of the evidence budget. `verify`/`task-verify` prepend `failures: <lines>` ahead of the existing tail-trimmed output only on a failed run, the summary budgeted first inside the same cap; a passing run's evidence, and a failing run whose output matches nothing, stay byte-identical to before.
- ~~**Dependabot configuration**~~ **DELIVERED by M017/T006** (raised post-M008). `.github/dependabot.yml`: weekly `npm` updates on `/`, `open-pull-requests-limit: 5`, dev-dependencies grouped into one PR, a header comment stating no auto-merge is configured anywhere. `README.md` gains a "Maintenance" note: updates arrive as Dependabot PRs; security alerts are a repository setting to enable, not done by this file.
- ~~**Completed-task usage correction**~~ **DELIVERED by M017/T003** (found M007/T013's verification, recurred at M009/T001). Direction chosen: completion-time detection/warning, not a new `usage-correct` command (a post-completion write would have no checkpoint to ride once the milestone completes). `task-update <id> completed` for a task with at least one `worktree_dispatch` journal record but no `--usage` emits a one-line stderr warning naming the task, and `--json` gains an additive `usageWarning: string | null` key — detection only, never estimated, never a refusal. Disclosed scope: covers `parallel_worktrees` dispatches only; an inline sub-agent dispatch leaves no journal record and is indistinguishable from driver-executed work.
- ~~**Task human-readable name field**~~ **DELIVERED by M013/T002** (raised M012/T009's own hardening review, 2026-08-20). `taskSchema` gained an additive-optional `name` field (short label, id-fallback), surfaced in `resume`/`task-status`/the Progress Report's task table, non-breaking for every pre-M013 task.
- ~~**Two real M011 dogfood structural findings, not yet fixed**~~ **DELIVERED by M016/T002** (`docs/evidence/M011/skill-dogfood-evidence.md`). The `QuickChangeError` class identity split between `create.ts` and `run.ts` unified to one canonical class; the `requireQuickChange` helper — found duplicated in FOUR files at fix time (`create.ts`, `run.ts`, `commit.ts`, AND `promote.ts`, one more than originally named) — consolidated to a single shared implementation. Dispatched and integrated as the milestone's own first real parallel-worktree dispatch.
- ~~**Actionable missing-`.pitway/`-state diagnostic**~~ **DELIVERED by M017/T005** (raised M013/T009). `src/state/store.ts`'s `readText`/`readInputFile` now preserve the underlying fs error as `cause`. `src/cli/errors.ts`'s `renderCliError` narrows to PitWay's own error classes (a non-builtin `Error` constructor name — never an exhaustive per-class import list) versus a builtin/unknown throw (which keeps its stack), and maps a `StateStoreError` whose cause is ENOENT on `.pitway/state.yaml` to "run `pitway init` in a repository root, or check out the branch that carries `.pitway/`." `src/cli/index.ts`'s main-module block wraps `parseAsync` in the one shared entry point this needed — real-subprocess-only, in-process tests unaffected.
- ~~**`write_scope`/stale-test-assertion gap pattern, fourth recurrence**~~ **DELIVERED by M017/T008** (found M012/T005, M013/T008, M014/T011, M015/T008). `protocol-driver.md` gains a "Drafting write_scope" section naming the concrete assertion sites to grep before finalizing any task that changes a count, enumerated list, or persisted/rendered string shape (the registered-command list, baseline/`completionPaths`, shipped-asset tests, any exact-string test) — a systemic checklist rather than continuing to absorb the pattern per-milestone. Docs-only.
- ~~**`milestone-confirm` never reconciles pending journal entries after its baseline commit**~~ **DELIVERED by M017/T001** (found live, M016's own execution). `reconcilePending(root, milestoneId)` now runs after `createBaselineCommit` in both of `confirm.ts`'s commit-producing paths (fresh confirm and resumed confirm), exactly as scoped — mirroring the existing `task-update`/`milestone-complete` call sites, byte-match semantics unchanged. Regression tests prove a milestone confirmed with matching pending entries (e.g. a pre-confirmation `milestone-review` session's `review_recording` entries) can `task-dispatch` immediately afterward, and that an entry whose target still differs from HEAD correctly stays pending.
- ~~**No sanctioned path to add a task to, or cancel/undo, a confirmed milestone.**~~ **DELIVERED by M017/T002, T008** (discovered live, M016's own execution). Decision made: insertion-yes/cancel-no. `task-add <milestone-id> --file <yaml> --change-log <text>` inserts one new task into a `confirmed`/`in_progress` milestone, reusing the existing `task_amendment` journal operation unchanged. `milestone-cancel` deliberately stays draft-only — no new state-machine edge; `in_progress` genuinely has none to `cancelled`, and `confirmed` is never an observable resting status (confirm collapses `draft → confirmed → in_progress` in one write) even though the state-machine table's `confirmed → cancelled` entry technically exists. `protocol-driver.md`/`commands/milestone-cancel.md` now document the boundary explicitly: abandon remaining work in a confirmed milestone by cancelling its not-yet-started tasks (`task-update <id> cancelled`) and completing with what's done — never `git reset`.
- ~~**Load-sensitive integration tests beyond the one M016/T001 fixed**~~ **DELIVERED by M017/T007** (found M016/T001's own real reproduction). Direction chosen: a single global `testTimeout`/`hookTimeout: 60000` in `vitest.config.ts`, not per-test overrides — one authoritative value covering `milestone-complete.test.ts`, `verification-repair.test.ts`, `execution-strategy.test.ts`, `review-state-lifecycle.test.ts`, and `branch-isolation-lifecycle.test.ts` alongside the already-fixed `completed-task-revision-path.test.ts`, whose own per-test `60000` override is now redundant and removed. Executed inline and second, before any parallel dispatch, so every later worktree inherits the config. **Explicitly left open**: the separately-observed `npm-pack.test.ts` transient is *not* claimed fixed — it was never a `Test timed out in 5000ms`-signature failure (the same signature every test above shared), and this milestone's own investigation found it passes cleanly standalone, consistent with load collateral rather than a timeout this config addresses. Still no milestone-assigned root cause of its own.

---

**Status:** authoritative current state is never this prose — run `pitway resume` (or `pitway milestone-status <id>`) for the live, always-current answer; `.pitway/state.yaml` is the underlying source of truth. This paragraph is a **point-in-time narrative snapshot as of M020's own execution (2026-08-22)**, kept for historical continuity, not as a live status feed — a later milestone's own completion does not retroactively make this paragraph's *narrative* wrong, only stale, and each future roadmap-reconciliation task updates it the same way rather than leaving it silently outdated (the same staleness pattern repeated across M005 through M009's own equivalent Status lines before M008's restructuring). M019 completed and merged into `main` (`5f8d36d`), plus two post-M019 `quick-change` commits landed on `main` before M020 began: `cbe1a15` (`qc-7e6fb2a4`, the missing `ms-merge` alias) and `3a297d9` (`qc-404ee3e9`, aligning `milestone-status`'s Progress Report task table with the plain report's own table+usage format — B005). M020 (CLI Branch Coverage Hardening) is `in_progress` at the time this reconciliation task runs, itself part of M020's own task graph.

As of this snapshot: M005 through M012 are each completed (`milestone-complete`); M006 landed three standalone hotfixes, M007 landed zero during its own execution (one further standalone commit, `chore: ignore local workflow reports`, landed between M006 and M007, classified as a between-milestone maintenance commit, never an eighth hotfix), M008 landed one disclosed post-completion exception (`6f8b5e6`/`ed119ed`, a verification repair with no supported command yet to receive it — the exact gap M009/AC002 closed), M009 landed zero standalone hotfix commits during its own execution (every ripple effect folded into its discovering task's own commit via an approved `task-amend`) and one disclosed post-completion exception (`13865d4`, a bootstrap-circularity fix to `verification-repairs.yaml`'s own store loader — no repair path existed yet to receive it, closed as a standalone developer-approved fix rather than through `quick-change`), M010 landed zero standalone hotfix commits during its own execution, M011 (Claude Skills) landed zero standalone hotfix commits during its own execution, M012 (Milestone Git branch isolation) landed zero standalone hotfix commits during its own execution and one task-level `write_scope` correction (T005, via developer-approved `task-amend`). M013 (PitWay Driver UX) is completed, its own eight tasks landing zero standalone hotfix commits and one developer-approved mid-execution scope widening (T008, via `task-amend`). M014 (Parallel task worktrees) is completed, its own eleven tasks landing two standalone developer-directed fixes between tasks (racing-footer name segment, footer output-separation tests, M013-surface polish outside M014's own scope) and building parallel dispatch while itself executing sequentially — the dogfood irony disclosed in its own Revised Roadmap entry. M015 (PitWay Milestone Review) is completed, its own twelve tasks delivering the role-based review workflow, landing two disclosed gate-caught defect fixes (`content_hash`'s scope, a stale `git-baseline.test.ts` assertion) via the established widening precedent and one drafting-gap fix (a missing `CT016` verification check). M016 (Extended Dogfood Validation and Release Readiness) is completed (`milestone-complete`, `0bc57c9`) on its own `pitway/M016-extended-dogfood-validation-and-release` branch — the first milestone executed under `git.branch_strategy: milestone` — and merged into `main` by the developer as `3ddff06` (a clean, conflict-free `--no-ff` merge; the branch is fully merged and still exists locally pending the developer's own deletion decision); see its Revised Roadmap entry above for delivered scope, including the first real parallel dispatch, the M011 findings fix, the flake root-cause and hardening, and the two genuine mechanism gaps found live and recorded in the Backlog. Because M016's baseline commit carried the `config.yaml` opt-ins, `main` now runs `git.branch_strategy: milestone` and `execution.strategy: parallel_worktrees` as standing policy, not a one-off. Between M016's branch point and its merge, three developer-requested `quick-change` commits landed directly on `main` with `PitWay-Change` trailers: `.gitignore` coverage for `pitway init`'s installed `.claude/` output and generated `AGENTS.md` (neither is source; the tracked source stays at `src/integrations/claude/`), a context-efficiency compaction of the seven non-command Claude integration docs (723 → 555 lines, every behavioral invariant preserved), and a resolution of a pre-existing wording conflict between `report-format.md`'s "full suite once at the end" and the M012 verification-discipline rule (workers now run only their assigned verification command; the full suite is a milestone gate unless the driver explicitly declares it as a task's own command). M017 (Backlog Closure and Lifecycle Hardening) is completed, its own nine tasks (T009 was this reconciliation itself, drafted last per the milestone's own execution order): `milestone-confirm`'s `reconcilePending` gap fixed, `task-add` delivered as the sanctioned confirmed-milestone task-insertion path (with `milestone-cancel` staying deliberately draft-only), a completion-time usage warning for worktree-dispatched tasks, structured failure-evidence extraction (`summarizeFailure`), a clean CLI error boundary with an actionable missing-state diagnostic, Dependabot configuration, a global `vitest.config.ts` timeout closing the load-sensitive-tests item (`npm-pack.test.ts`'s own transient explicitly left open, unfixed), and a "Drafting write_scope" protocol-driver.md checklist — closing all eight of the Backlog's still-open, milestone-unassigned items in one pass, landing zero standalone hotfix commits during its own execution. M018 (Backlog / Deferred Work Capture) is completed, its own six tasks delivering `pitway backlog add|list|show|promote|archive` — the controlled, `.pitway/backlog.yaml`-authoritative mechanism the now-superseded "Backlog / Pending Candidates" section above existed to work around before this milestone. A pre-implementation `milestone-review` (architect role) caught and closed a real safety gap in the drafted design before any code was written: the original contract mirrored `task-amend`'s explicit-override-or-fallback pattern for journal attachment, unsafe here because `.pitway/backlog.yaml` is the first journal-backed target not exclusively owned by one milestone's directory — revised to unconditional active-milestone attachment with no override at all. Two task-level `write_scope` drafting-gap corrections (T005, via `task-amend`) — one path named the gitignored `.claude/protocol-driver.md` instead of its real tracked source, the other named the gitignored `.claude/commands/backlog.md` itself, which `task-verify` correctly refused. Real dogfooding, not fabricated: four genuine backlog items (B001–B004) were captured via the actual built CLI during this milestone's own execution, each a real developer request kept explicitly out of M018's own scope rather than absorbed. Zero standalone hotfix commits during its own execution. M019 (Milestone Merge Workflow and Backlog Promotion) is completed, its own seven tasks delivering `pitway milestone-merge` (the manual-merge gap closed for real, T001), the first real exercise of `backlog promote` (B001–B003 promoted into T003/T004/T005 respectively), and a driver-agnostic runtime usage-propagation MUST-requirement (T007, inserted directly by developer instruction and run first). A pre-confirm `milestone-review` (architect + devops) caught and closed two real gaps in `milestone-merge`'s design before any code was written. One pre-existing, uncommitted developer edit (the workflow diagram) was folded into T006 by explicit decision rather than committed separately; one dispatched worker's incomplete turn was resumed rather than trusted as done; one task-level `write_scope` drafting bug (T003, the same class M018/T005 hit) was self-caught and fixed via `task-amend`; one transient system-resource-contention verification hang was confirmed not a code defect via reproduction before completing. Zero standalone hotfix commits during its own execution. Full details and evidence for each milestone are recorded in `reports/*.md` (local, non-authoritative, supplementary only — never a source for a public claim) and `docs/evidence/**` (committed, authoritative). Structured failure-evidence extraction is built for real (M017), quick-change is built for real (M009), Claude skills is built for real (M011), milestone-level Git branch strategy is built for real (M012), the permanent driver UX (racing footer, Progress Report, task name field) is built for real (M013), parallel task worktrees are built for real (M014), role-based milestone review is built for real (M015), the confirmed-milestone task-insertion path is built for real (M017), the deferred-work backlog is built for real (M018), and the milestone-merge/backlog-promote/usage-propagation trio is built for real (M019) — none remains an unattributed placeholder. The interim policy from M007 (out-of-scope defects use a one-task corrective milestone, not an unattributed standalone hotfix commit) remains in effect for anything quick-change's own bounded-scope gates don't cover.
